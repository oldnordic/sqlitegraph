---
phase: 04-mvcc-completion
plan: 01
type: execute
depends_on: []
files_modified: [sqlitegraph/src/mvcc.rs, sqlitegraph/src/backend.rs]
autonomous: true
---

<objective>
Identify and document MVCC gaps in the current implementation.

Purpose: Create a comprehensive analysis of the current MVCC-lite snapshot system, identify concurrency issues, document edge cases, and establish baseline metrics.
Output: MVCC gap analysis document with identified issues and test scenarios.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key Finding from Code Analysis
Current MVCC implementation (mvcc.rs) uses ArcSwap for lock-free atomic updates and stores cloned HashMaps for isolation. However, there are NO true concurrent access tests - all existing tests are single-threaded.

# Relevant Source Files
@sqlitegraph/src/mvcc.rs
@sqlitegraph/src/backend.rs
@sqlitegraph/tests/mvcc_snapshot_tests.rs
@sqlitegraph/src/backend/native/v2/snapshot/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze current MVCC implementation and identify gaps</name>
  <files>docs/MVCC_GAP_ANALYSIS.md (new)</files>
  <action>
Create comprehensive MVCC gap analysis document:

1. **Current Implementation Review**:
   - Document ArcSwap-based snapshot manager architecture
   - Analyze SnapshotState structure (cloned HashMaps)
   - Review GraphSnapshot isolation guarantees
   - Identify integration points with SqliteGraph backend

2. **Identified Gaps**:
   - No concurrent read/write tests (all tests single-threaded)
   - Missing snapshot lifecycle integration with Native V2
   - No stress testing for concurrent snapshot acquisition
   - Undefined behavior: snapshot during WAL recovery
   - Undefined behavior: snapshot during checkpoint
   - Missing: snapshot memory pressure handling
   - Missing: snapshot cleanup and resource management

3. **Concurrency Issues**:
   - RwLock usage in hnsw_indexes (potential contention)
   - No testing of simultaneous reads + writes
   - No testing of snapshot acquisition during state updates

4. **Edge Cases**:
   - Empty graph snapshots
   - Large graph snapshots (memory pressure)
   - Rapid snapshot creation/expiration
   - Snapshot during transaction rollback
   - Snapshot during WAL checkpoint

5. **Missing Integration**:
   - SnapshotManager not integrated with SqliteGraph public API
   - No snapshot isolation for Native V2 backend
   - WAL operations don't coordinate with snapshots

Document each gap with:
- Severity (Critical/High/Medium/Low)
- Impact description
- Recommended fix approach
- Test scenarios needed

WHY: Understanding gaps is essential before implementing fixes. This analysis guides Plans 02 and 03.
  </action>
  <verify>
docs/MVCC_GAP_ANALYSIS.md exists and contains:
- Current implementation review section
- Identified gaps with severity ratings
- Concurrency issues documented
- Edge case catalog
- Missing integration points
  </verify>
  <done>
MVCC gap analysis document created with:
- 10+ identified gaps categorized by severity
- Concurrency issue analysis
- Edge case catalog with test scenarios
- Integration gap documentation
- Recommendations for each gap
  </done>
</task>

<task type="auto">
  <name>Task 2: Create baseline MVCC test infrastructure</name>
  <files>sqlitegraph/tests/mvcc_baseline_tests.rs (new)</files>
  <action>
Create baseline MVCC test infrastructure to measure current behavior:

1. **Single-threaded baseline tests** (verify current functionality):
   ```rust
   #[test]
   fn test_snapshot_isolation_single_threaded() {
       // Verify snapshot sees consistent state
       // Modify graph after snapshot
       // Verify snapshot unchanged
   }
   ```

2. **Snapshot lifecycle tests**:
   - Creation and basic access
   - Multiple snapshots from same base
   - Snapshot ordering and consistency

3. **Memory footprint tests**:
   - Measure memory per snapshot
   - Test with large graphs (10K+ nodes)
   - Verify no memory leaks

4. **Performance baseline tests**:
   - Snapshot acquisition latency
   - Snapshot clone performance
   - Multiple snapshot overhead

5. **Integration tests**:
   - Snapshot with SQLite backend
   - Snapshot with Native V2 backend (if available)

Use criterion for microbenchmarks where appropriate.

AVOID: Multi-threaded tests in this task - those come in Plan 02.

DO: Document current behavior even if buggy - this is the baseline.
  </action>
  <verify>
cargo test --package sqlitegraph mvcc_baseline_tests passes
All tests establish measurable baselines:
- Snapshot isolation verified
- Memory footprint measured
- Performance baseline established
- Integration with both backends tested
  </verify>
  <done>
Baseline test infrastructure created with:
- 15+ single-threaded tests
- Memory footprint measurements
- Performance benchmarks using criterion
- SQLite and Native V2 integration tests
- Baseline metrics documented
  </done>
</task>

<task type="auto">
  <name>Task 3: Document test scenarios for concurrent operations</name>
  <files>docs/MVCC_TEST_SCENARIOS.md (new)</files>
  <action>
Create comprehensive test scenario documentation for Plan 02 (concurrent tests):

1. **Race Condition Scenarios**:
   - Concurrent snapshot acquisition during state update
   - Read during write (one writer, multiple readers)
   - Snapshot acquisition during WAL checkpoint
   - Multiple snapshots created simultaneously

2. **Stress Test Scenarios**:
   - 100 threads acquiring snapshots simultaneously
   - Rapid snapshot creation/destruction cycles
   - Large graph snapshot under memory pressure
   - Sustained concurrent reads + writes

3. **Correctness Scenarios**:
   - Verify snapshot isolation guarantees
   - Verify no data races in concurrent access
   - Verify snapshot consistency under concurrent writes
   - Verify ArcSwap atomic guarantees

4. **Performance Scenarios**:
   - Snapshot contention under high load
   - Reader/writer priority handling
   - Cache coherency under concurrent access

For each scenario, document:
- Test objective
- Setup requirements
- Success criteria
- Expected behavior (if known)
- How to detect failures (data races, deadlocks, etc.)

This document becomes the specification for Plan 02's implementation.
  </action>
  <verify>
docs/MVCC_TEST_SCENARIOS.md exists and contains:
- 20+ concurrent test scenarios
- Race condition scenarios documented
- Stress test scenarios with thread counts
- Correctness verification criteria
- Performance measurement approaches
  </verify>
  <done>
Test scenario document created with:
- 20+ concurrent operation test scenarios
- Race condition detection strategies
- Stress test specifications (100+ threads)
- Correctness validation criteria
- Performance measurement approaches
- Ready for implementation in Plan 02
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docs/MVCC_GAP_ANALYSIS.md created with comprehensive gap analysis
- [ ] docs/MVCC_TEST_SCENARIOS.md created with test scenario specifications
- [ ] sqlitegraph/tests/mvcc_baseline_tests.rs created and passing
- [ ] cargo test --package sqlitegraph passes (no regressions)
- [ ] Memory and performance baselines documented
</verification>

<success_criteria>
- MVCC gap analysis complete with 10+ identified issues
- Baseline test infrastructure establishes current behavior metrics
- Test scenarios documented for concurrent operations
- All baseline tests passing
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-01-SUMMARY.md`
</output>
