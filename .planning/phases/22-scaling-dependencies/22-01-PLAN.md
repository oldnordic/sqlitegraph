---
phase: 22-scaling-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sqlitegraph/src/backend/native/v2/wal/checkpoint/core.rs
  - sqlitegraph/src/backend/native/v2/wal/checkpoint/io/checkpoint_writer.rs
  - sqlitegraph/src/backend/native/v2/wal/checkpoint/io/mod.rs
  - sqlitegraph/src/backend/native/v2/wal/checkpoint/constants.rs
autonomous: true

must_haves:
  truths:
    - "Checkpoint supports files larger than 1GB"
    - "Multi-file checkpoint creates consistent segments with sequence numbers"
    - "Checkpoint recovery handles multi-part checkpoints atomically"
    - "Large checkpoint tests verify correctness with 2GB+ databases"
  artifacts:
    - path: "sqlitegraph/src/backend/native/v2/wal/checkpoint/io/multi_file.rs"
      provides: "Multi-file checkpoint coordination"
      min_lines: 150
    - path: "sqlitegraph/src/backend/native/v2/wal/checkpoint/core.rs"
      contains: "MultiFileCheckpoint"
      exports: ["create_multi_file_checkpoint", "recover_multi_file_checkpoint"]
    - path: "sqlitegraph/src/backend/native/v2/wal/checkpoint/constants.rs"
      contains: "MAX_CHECKPOINT_SIZE"
  key_links:
    - from: "MultiFileCheckpoint::create"
      to: "checkpoint_writer.rs"
      via: "CheckpointWriter::write_header"
      pattern: "write_segment_header"
    - from: "checkpoint recovery"
      to: "multi_file segments"
      via: "segment sequence validation"
      pattern: "validate_segment_sequence"
---

<objective>
Implement multi-file checkpointing for databases larger than 1GB

Purpose: Current checkpoint implementation is limited to single 1GB files (MAX_CHECKPOINT_SIZE). Large databases need multi-file checkpoint support to scale beyond this limit.
Output: Multi-file checkpoint system that creates consistent segment files, atomic recovery, and tests for 2GB+ databases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-scaling-dependencies/22-RESEARCH.md

# Existing checkpoint code
@sqlitegraph/src/backend/native/v2/wal/checkpoint/core.rs
@sqlitegraph/src/backend/native/v2/wal/checkpoint/io/checkpoint_writer.rs
@sqlitegraph/src/backend/native/v2/wal/checkpoint/constants.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-file checkpoint module</name>
  <files>sqlitegraph/src/backend/native/v2/wal/checkpoint/io/multi_file.rs</files>
  <action>
    Create new module `multi_file.rs` in checkpoint/io directory with:

    1. `MultiFileCheckpointConfig` struct:
       - max_segment_size: u64 (default 512MB)
       - base_path: PathBuf
       - max_segments: usize (default 16 = 8GB max)

    2. `CheckpointSegment` struct:
       - segment_index: u32
       - segment_path: PathBuf
       - lsn_range: (u64, u64)
       - block_count: u64
       - checksum: u64

    3. `SegmentWriter` struct with:
       - `create_segment(config, index, lsn_start)` -> Result<SegmentWriter>
       - `write_data(&mut self, data: &[u8])` -> CheckpointResult<usize>
       - `finalize(&mut self, lsn_end, block_count)` -> CheckpointResult<CheckpointSegment>
       - Automatic segment rotation when size threshold exceeded

    4. `SegmentReader` struct with:
       - `open_segment(path)` -> CheckpointResult<SegmentReader>
       - `read_data(&mut self, buf: &mut [u8])` -> CheckpointResult<usize>
       - `validate_checksum(&self)` -> CheckpointResult<bool>

    Segment file naming pattern: `{base}.ckpt.{index:03d}`
    Use CRC32 for segment checksums (fast, sufficient for crash detection)
  </action>
  <verify>
    cargo test --package sqlitegraph --lib checkpoint::io::multi_file::tests
  </verify>
  <done>
    Module compiles with all unit tests passing. Segment writer can create and rotate segments. Segment reader validates checksums.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate multi-file support into checkpoint manager</name>
  <files>sqlitegraph/src/backend/native/v2/wal/checkpoint/core.rs</files>
  <action>
    Modify `V2WALCheckpointManager` to support multi-file checkpoints:

    1. Add field to V2WALCheckpointManager:
       ```rust
       multi_file_config: Option<Arc<Mutex<MultiFileCheckpointConfig>>>
       ```

    2. Modify `execute_checkpoint()` method:
       - Check if multi_file_config is Some
       - If enabled, use SegmentWriter instead of single CheckpointWriter
       - Track segment index and rotate when segment exceeds max_segment_size
       - Write checkpoint manifest file `{base}.ckpt.manifest` with:
         * segment_count: u32
         * segments: Vec<CheckpointSegmentMeta>
         * total_lsn_range: (u64, u64)
         * total_block_count: u64
         * checksum: u64 (over entire manifest)

    3. Add `create_multi_file()` builder method:
       ```rust
       pub fn with_multi_file(config: MultiFileCheckpointConfig) -> Self
       ```

    4. Update checkpoint state transition to handle multi-file completion:
       - Write manifest after all segments finalized
       - Validate all segments exist before marking Complete

    Reference existing checkpoint flow in execute_checkpoint() (line 651-682 in core.rs).
  </action>
  <verify>
    cargo test --package sqlitegraph --lib checkpoint::tests::test_multi_file_checkpoint
  </verify>
  <done>
    Checkpoint manager creates multiple segments for large databases. Manifest file written atomically after all segments.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement multi-file checkpoint recovery</name>
  <files>sqlitegraph/src/backend/native/v2/wal/checkpoint/io/multi_file.rs</files>
  <action>
    Add recovery functions to multi_file.rs:

    1. `CheckpointManifest` struct for recovery:
       ```rust
       pub struct CheckpointManifest {
           pub segment_count: u32,
           pub segments: Vec<CheckpointSegmentMeta>,
           pub total_lsn_range: (u64, u64),
           pub total_block_count: u64,
           pub checksum: u64,
       }
       ```

    2. `MultiFileRecovery` struct with methods:
       - `discover_checkpoints(base_path)` -> Vec<PathBuf> (find all .ckpt.manifest files)
       - `load_manifest(path)` -> CheckpointResult<CheckpointManifest>
       - `validate_checkpoint(manifest)` -> CheckpointResult<bool>
         * Verify all segment files exist
         * Validate each segment checksum
         * Verify manifest checksum
         * Verify LSN continuity across segments
       - `recover_checkpoint(manifest)` -> CheckpointResult<RecoveredCheckpoint>
         * Open all segments in order
         * Return iterator over segment data

    3. Add to checkpoint/mod.rs:
       ```rust
       pub mod multi_file;
       pub use multi_file::{MultiFileRecovery, CheckpointManifest};
       ```

    Reference existing recovery patterns in wal/recovery module.
  </action>
  <verify>
    cargo test --package sqlitegraph --lib checkpoint::io::multi_file::tests::test_recovery
  </verify>
  <done>
    Multi-file recovery discovers manifests, validates all segments, and recovers checkpoint data atomically.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add large checkpoint scaling tests</name>
  <files>sqlitegraph/tests/large_checkpoint_test.rs</files>
  <action>
    Create integration test file `tests/large_checkpoint_test.rs` with:

    1. `test_checkpoint_exceeds_1gb()`:
       - Create V2 database with ~1.5GB of data
       - Trigger checkpoint
       - Verify multiple segments created
       - Verify manifest valid
       - Recover and verify data integrity

    2. `test_checkpoint_segment_rotation()`:
       - Set max_segment_size to 10MB (small for testing)
       - Create database with 50MB data
       - Verify 5 segments created
       - Verify LSN continuity

    3. `test_recovery_partial_segments()`:
       - Create multi-file checkpoint
       - Delete one segment file
       - Verify recovery fails with proper error
       - Restore segment
       - Verify recovery succeeds

    4. `test_concurrent_checkpoint_limit()`:
       - Verify MAX_CONCURRENT_CHECKPOINTS = 1 enforced for multi-file

    Use tempfile for test databases. Clean up after each test.

    Note: These tests use smaller sizes for CI but demonstrate the scaling pattern works.
  </action>
  <verify>
    cargo test --test large_checkpoint_test
  </verify>
  <done>
    All 4 large checkpoint tests pass. Multi-file checkpoint scales beyond 1GB limit.
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Multi-file checkpoint module exists and compiles
2. Checkpoint manager integrates multi-file support
3. Recovery handles multi-part checkpoints atomically
4. Tests verify 2GB+ database checkpoint behavior
</verification>

<success_criteria>
- Checkpoint supports files larger than 1GB (verified by test creating 1.5GB checkpoint)
- Multi-file checkpoint creates consistent segments with sequence numbers
- Checkpoint recovery handles multi-part checkpoints atomically
- SCALE-CP-01, SCALE-CP-02, SCALE-CP-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/22-scaling-dependencies/22-01-SUMMARY.md`
</output>
