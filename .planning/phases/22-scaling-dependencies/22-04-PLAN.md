---
phase: 22-scaling-dependencies
plan: 04
type: execute
wave: 3
depends_on: [22-01, 22-02, 22-03]
files_modified:
  - .planning/phases/22-scaling-dependencies/22-04-PLAN.md
  - docs/SCALING.md
  - docs/MIGRATION.md
autonomous: true

must_haves:
  truths:
    - "Dependency monitoring document exists with rusqlite and bincode status"
    - "bincode 2.0 migration plan documents breaking changes and compatibility"
    - "HNSW disk-based option research documents migration path"
    - "All scaling limits are documented with mitigation strategies"
  artifacts:
    - path: "docs/SCALING.md"
      provides: "Scaling limits documentation"
      min_lines: 100
      contains: "checkpoint", "dirty_blocks", "transaction_id", "hnsw"
    - path: "docs/MIGRATION.md"
      provides: "Dependency migration documentation"
      min_lines: 80
      contains: "bincode 2.0", "rusqlite", "breaking changes"
    - path: ".planning/phases/22-scaling-dependencies/22-04-PLAN.md"
      contains: "monitoring", "migration", "deferred"
  key_links:
    - from: "SCALING.md"
      to: "codebase constants"
      via: "references to MAX_GLOBAL_DIRTY_BLOCKS, etc"
      pattern: "MAX_.*_BLOCKS"
    - from: "MIGRATION.md"
      to: "Cargo.toml"
      via: "dependency version references"
      pattern: "bincode.*=\\s*\"1"
---

<objective>
Document dependency monitoring and bincode 2.0 migration plan

Purpose: bincode development ceased (Dec 2025) - community forks available for 2.0. rusqlite 0.31 uses bundled SQLite for security. HNSW disk-based indexing is complex - defer to v2.
Output: Documentation for scaling limits, dependency health monitoring, and bincode migration path.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-scaling-dependencies/22-RESEARCH.md

# Current dependencies
@sqlitegraph/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scaling limits documentation</name>
  <files>docs/SCALING.md</files>
  <action>
    Create docs/SCALING.md documenting all known scaling limits:

    ```markdown
    # SQLiteGraph Scaling Limits

    This document describes known scaling limits and their mitigation strategies.

    ## Checkpoint Scaling

    ### Current Limit: 1GB per checkpoint file
    - **Constant**: `MAX_CHECKPOINT_SIZE = 1024 * 1024 * 1024` (constants.rs:22)
    - **Impact**: Databases larger than 1GB require multi-file checkpoint
    - **Mitigation**: Multi-file checkpoint implemented in Phase 22-01
    - **Status**: RESOLVED - Multi-file checkpoint supports unlimited size

    ### Multi-File Checkpoint
    - **Segment Size**: 512MB default (configurable)
    - **Max Segments**: 16 default = 8GB max (configurable)
    - **Recovery**: Atomic via manifest file validation
    - **Reference**: `checkpoint/io/multi_file.rs`

    ## Dirty Block Tracking

    ### Current Limit: 50,000 global dirty blocks
    - **Constant**: `MAX_GLOBAL_DIRTY_BLOCKS = 50_000` (constants.rs:34)
    - **Per-Cluster Limit**: `MAX_DIRTY_BLOCKS_PER_CLUSTER = 10_000`
    - **Impact**: High write workloads may hit limit
    - **Mitigation**: Overflow strategies implemented in Phase 22-02
    - **Status**: RESOLVED - 4 overflow strategies available

    ### Overflow Strategies
    1. **Reject**: Returns error (default, backward compatible)
    2. **ForceCheckpoint**: Triggers checkpoint automatically
    3. **SpillToDisk**: Spills oldest blocks to disk
    4. **HierarchicalPromotion**: Promotes to cluster-affinity tracking

    ## Transaction ID Management

    ### Current Limit: u64::MAX theoretical
    - **Type**: `TransactionId = u64` (transaction_coordinator.rs:17)
    - **Wraparound Protection**: 1M transaction safety margin
    - **Warning Threshold**: u64::MAX - 11M
    - **Impact**: No practical limit for production workloads
    - **Mitigation**: Wraparound protection implemented in Phase 22-03
    - **Status**: RESOLVED - TransactionIdManager enforces bounds

    ### Deadlock Detector Growth
    - **Unbounded**: Wait-for graph grows with concurrent transactions
    - **Cleanup Threshold**: 1000 entries triggers cleanup
    - **Mitigation**: Periodic cleanup of completed transactions
    - **Status**: RESOLVED - Cleanup implemented in Phase 22-03

    ## HNSW Vector Index

    ### Current Limit: In-memory only
    - **Storage**: `InMemoryVectorStorage` (hnsw/storage.rs)
    - **Overhead**: ~30% beyond raw vector data
    - **Impact**: Index size limited by available RAM
    - **Mitigation**: DEFERRED to v2 - DiskANN or HNSW disk spill

    ### Disk-Based HNSW Options (Research)
    1. **HNSW with disk spill**: Hybrid approach (complex implementation)
    2. **DiskANN**: Better for pure disk-based (less mature Rust ecosystem)
    3. **SQLite BLOB storage**: Already available for vector persistence
    - **Status**: DEFERRED - Documented migration path in MIGRATION.md

    ## Database File Size

    ### Native V2 Backend
    - **Theoretical Limit**: 16 EB (u64 file offsets)
    - **Practical Limit**: Disk space and checkpoint duration
    - **Recommendation**: Monitor checkpoint duration, use multi-file for >1GB

    ### SQLite Backend
    - **Limit**: 281 TB (SQLite limits)
    - **Practical Limit**: Host filesystem

    ## Monitoring Recommendations

    ### Metrics to Track
    1. Dirty block count: `dirty_blocks.global_dirty_blocks().len()`
    2. Checkpoint file size: `manager.get_wal_size()`
    3. Transaction ID consumption: `tx_id_manager.remaining_ids()`
    4. Deadlock graph size: `detector.graph_size()`

    ### Alerts
    - Dirty blocks > 40,000 (80% of limit)
    - Transaction IDs < 100M remaining
    - Checkpoint duration > 10 minutes
    - Deadlock graph size > 1000

    ## Future Work (v2+)

    1. **HNSW Disk-Based Index**: Evaluate DiskANN vs HNSW disk spill
    2. **Incremental Checkpoint**: Reduce checkpoint duration for large files
    3. **Parallel Dirty Block Tracking**: Sharded tracking for higher throughput
    ```

    Include references to constants in source code.
  </action>
  <verify>
    test -f docs/SCALING.md && wc -l docs/SCALING.md
  </verify>
  <done>
    SCALING.md exists with >100 lines documenting all scaling limits and mitigations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dependency migration documentation</name>
  <files>docs/MIGRATION.md</files>
  <action>
    Create docs/MIGRATION.md documenting dependency migration paths:

    ```markdown
    # Dependency Migration Guide

    This document tracks dependency health and migration plans for SQLiteGraph.

    ## bincode 1.3 -> 2.0 Migration

    ### Status: PLANNED (Deferred to v1.2+)

    ### Background
    - **Current**: bincode 1.3 (development ceased Dec 2025)
    - **Issue**: Original author halted development due to harassment
    - **Community**: Forks available (cu-bincode, bincode_next)

    ### Breaking Changes (bincode 2.0)
    1. **API Changes**:
       - `serialize()` returns `Result<Vec<u8>>` instead of `Vec<u8>`
       - `deserialize()` takes `&[u8]` instead of `&[u8; N]` for fixed-size
       - Configuration-based features (e.g., variable integer encoding)

    2. **Format Changes**:
       - Different binary format (NOT backward compatible)
       - Requires file format version bump (v3 -> v4)

    ### Migration Plan
    1. **Phase 1**: Add feature flag `bincode2`
       - Keep bincode 1.3 as default
       - Add conditional compilation for 2.0

    2. **Phase 2**: Implement format migration
       - Auto-detect v3 files (bincode 1.3)
       - Convert to v4 format (bincode 2.0)
       - Maintain read compatibility

    3. **Phase 3**: Switch default
       - Make bincode 2.0 default after stabilization period

    ### Estimated Effort
    - 2-3 days for implementation
    - 1 day for testing
    - 1 week soak period before switching default

    ## rusqlite Dependency

    ### Status: HEALTHY

    ### Current Version: 0.31
    - **Features**: bundled (SQLite 3.x compiled in)
    - **Rationale**: Bundled SQLite ensures security patches included
    - **System SQLite**: Available but NOT recommended (version uncertainty)

    ### Monitoring
    - Track rusqlite releases for security updates
    - Current: https://crates.io/crates/rusqlite
    - SQLite releases: https://sqlite.org/releaselog/index.html

    ### Future Versions
    - rusqlite 0.32+ requires r2d2_sqlite 0.38+ (breaking change)
    - Monitor r2d2_sqlite compatibility before upgrading

    ## HNSW Disk-Based Migration

    ### Status: RESEARCH (Deferred to v2)

    ### Options Evaluated
    1. **Custom HNSW Disk Spill**:
       - Pros: Consistent with current architecture
       - Cons: Complex implementation, replay on load

    2. **DiskANN**:
       - Pros: Designed for disk-based indexes
       - Cons: Less mature Rust ecosystem, different API

    3. **SQLite Vector Storage**:
       - Pros: Already available, ACID
       - Cons: Not optimized for vector workloads

    ### Recommended Path (v2)
    1. Benchmark DiskANN vs HNSW disk spill for typical workloads
    2. Evaluate separate vector database (e.g., separate SQLite DB)
    3. Consider memory-mapped vector storage for large indexes

    ## Dependency Health Checklist

    | Dependency | Version | Status | Action Needed |
    |------------|---------|--------|---------------|
    | rusqlite | 0.31 | Healthy | Monitor for updates |
    | bincode | 1.3 | Deprecated | Plan 2.0 migration |
    | r2d2_sqlite | 0.24 | Healthy | Compatible with rusqlite 0.31 |
    | parking_lot | 0.12 | Healthy | Stable API |
    | serde | 1.0 | Healthy | Stable API |
    | rayon | 1.10 | Healthy | Stable API |

    ## Monitoring Strategy

    1. **Weekly**: Check crates.io for dependency updates
    2. **Monthly**: Review security advisories (RustSec, GitHub ADP)
    3. **Quarterly**: Evaluate dependency updates for breaking changes
    4. **Per Release**: Run `cargo-audit` to detect vulnerabilities

    ### cargo-audit Integration
    ```bash
    # Install cargo-audit
    cargo install cargo-audit

    # Run security audit
    cargo audit
    ```

    Add to CI/CD pipeline for automated security checking.
    ```

    Include specific references to Cargo.toml versions.
  </action>
  <verify>
    test -f docs/MIGRATION.md && wc -l docs/MIGRATION.md
  </verify>
  <done>
    MIGRATION.md exists with >80 lines documenting dependency migration plans.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document HNSW disk-based migration research</name>
  <files>docs/MIGRATION.md</files>
  <action>
    Add HNSW disk-based section to MIGRATION.md (after the bincode section):

    ```markdown
    ## HNSW Disk-Based Migration (v2+)

    ### Current State (v1.1)
    - **In-Memory Graph**: Full HNSW structure in RAM
    - **Vector Persistence**: Vectors stored in SQLite BLOB
    - **Rebuild on Load**: Graph structure rebuilt from vectors
    - **Limitation**: ~30% overhead + vector data must fit in RAM

    ### Disk-Based Options

    #### Option 1: Hybrid HNSW with Disk Spill
    **Architecture**:
    - Hot nodes (recently accessed) in memory
    - Cold nodes spilled to disk (SQLite BLOB)
    - LRU cache for hot node promotion

    **Pros**:
    - Maintains current HNSW algorithm
    - Gradual migration path
    - Familiar SQLite storage

    **Cons**:
    - Complex cache management
    - Disk I/O during search
    - Cache miss performance penalty

    **Implementation Estimate**: 3-5 days

    #### Option 2: DiskANN Integration
    **Architecture**:
    - Replace HNSW with DiskANN entirely
    - Separate disk-optimized index

    **Pros**:
    - Designed for disk-based indexes
    - Better large-scale performance

    **Cons**:
    - Less mature Rust ecosystem
    - Breaking API change
    - Different algorithm semantics

    **Implementation Estimate**: 5-7 days

    #### Option 3: Separate Vector Database
    **Architecture**:
    - Use separate vector DB (e.g., sqlite-vec extension)
    - SQLiteGraph handles graph structure only

    **Pros**:
    - Leverages optimized vector storage
    - Separation of concerns

    **Cons**:
    - Additional dependency
    - Coordination complexity

    **Implementation Estimate**: 2-3 days

    ### Recommended Decision Matrix
    | Use Case | Recommendation |
    |----------|----------------|
    | < 1M vectors, RAM available | Current in-memory HNSW |
    | 1M-10M vectors | Hybrid HNSW with disk spill |
    | > 10M vectors | DiskANN or separate vector DB |

    ### v2 Decision Criteria
    - User workload data (vector count, RAM availability)
    - Benchmark results for each option
    - Community feedback on scaling needs
    ```

    Add to existing MIGRATION.md file (don't overwrite).
  </action>
  <verify>
    grep -q "DiskANN" docs/MIGRATION.md && grep -q "hybrid" docs/MIGRATION.md
  </verify>
  <done>
    MIGRATION.md includes HNSW disk-based migration research with 3 options documented.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add monitoring integration for dependency health</name>
  <files>sqlitegraph/src/lib.rs</files>
  <action>
    Add dependency health monitoring module to lib.rs:

    1. Add module declaration:
       ```rust
       #[cfg(feature = "dependency-monitoring")]
       pub mod dependency_monitor;
       ```

    2. Create `src/dependency_monitor.rs`:
       ```rust
       //! Dependency health monitoring utilities
       //!
       //! This module provides runtime monitoring of dependency versions
       //! and health status for production deployments.

       use std::collections::HashMap;

       #[derive(Debug, Clone)]
       pub struct DependencyInfo {
           pub name: &'static str,
           pub version: &'static str,
           pub status: DependencyStatus,
           pub minimum_version: Option<&'static str>,
       }

       #[derive(Debug, Clone, PartialEq, Eq)]
       pub enum DependencyStatus {
           Healthy,
           Deprecated,
           SecurityAdvisory,
                       Unknown,
       }

       pub fn get_dependency_info() -> HashMap<&'static str, DependencyInfo> {
           let mut deps = HashMap::new();

           deps.insert("rusqlite", DependencyInfo {
               name: "rusqlite",
               version: env!("CARGO_PKG_VERSION"),
               status: DependencyStatus::Healthy,
               minimum_version: None,
           });

           deps.insert("bincode", DependencyInfo {
               name: "bincode",
               version: "1.3",
               status: DependencyStatus::Deprecated,
               minimum_version: Some("2.0"),
           });

           // Add other dependencies as needed

           deps
       }

       pub fn print_dependency_report() {
           println!("=== SQLiteGraph Dependency Report ===");
           for (name, info) in get_dependency_info() {
               println!("{}: {} ({:?})", info.name, info.version, info.status);
               if let Some(min_ver) = info.minimum_version {
                   println!("  -> Migration recommended: >= {}", min_ver);
               }
           }
       }
       ```

    3. Add feature flag to Cargo.toml:
       ```toml
       dependency-monitoring = []  # Enable dependency health monitoring
       ```

    This is a lightweight monitoring feature - production users can enable via feature flag.
  </action>
  <verify>
    cargo check --features dependency-monitoring --package sqlitegraph --lib
  </verify>
  <done>
    Dependency monitoring module compiles with feature flag.
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. SCALING.md documents all scaling limits with mitigations
2. MIGRATION.md documents bincode 2.0 and HNSW migration paths
3. Dependency monitoring code exists
4. DEP-RUST-01, DEP-RUST-02, DEP-BIN-01, DEP-BIN-02, SCALE-HNSW-01, SCALE-HNSW-02 satisfied
</verification>

<success_criteria>
- Dependency monitoring document exists
- bincode 2.0 migration plan documented
- HNSW disk-based option research documented
- All scaling limits documented
</success_criteria>

<output>
After completion, create `.planning/phases/22-scaling-dependencies/22-04-SUMMARY.md`
</output>
