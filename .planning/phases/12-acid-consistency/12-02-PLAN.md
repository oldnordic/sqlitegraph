---
phase: 12-acid-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sqlitegraph/src/backend/native/v2/wal/checkpoint/validation/invariants.rs
  - sqlitegraph/src/backend/native/v2/wal/checkpoint/core.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Checkpoint state validation works with CheckpointState enum (not a struct with fields)"
    - "State transition validation ensures only valid state machine transitions occur"
    - "Checkpoint metadata is accessed from CheckpointManagerState, not CheckpointState enum"
    - "Validation runs during checkpoint Validating state without compilation errors"
  artifacts:
    - path: "sqlitegraph/src/backend/native/v2/wal/checkpoint/validation/invariants.rs"
      provides: "V2InvariantValidator with working checkpoint state validation"
      min_lines: 30
    - path: "sqlitegraph/src/backend/native/v2/wal/checkpoint/core.rs"
      provides: "CheckpointManagerState with metadata needed for validation"
      contains: "CheckpointManagerState"
  key_links:
    - from: "V2InvariantValidator::validate_checkpoint_state_invariants()"
      to: "CheckpointManagerState"
      via: "pass state metadata as parameter"
      pattern: "validate_checkpoint_state_invariants.*CheckpointManagerState"
---

<objective>
Fix checkpoint state validation to work with the actual CheckpointState enum structure.

Purpose: The existing checkpoint state validation code (invariants.rs lines 236-278) is commented out because it was written for a struct-based CheckpointState with fields like `in_progress`, `checkpoint_file_path`, `start_lsn`, `end_lsn`, `v2_metadata`. The actual CheckpointState is a simple enum with variants: Idle, Initializing, Collecting, Processing, Flushing, Validating, Complete, Failed. Validation needs to work with the enum and access metadata from CheckpointManagerState instead.

Output: Checkpoint state validation that detects corrupted checkpoint state without compilation errors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-acid-consistency/12-RESEARCH.md
@.planning/STATE.md

@sqlitegraph/src/backend/native/v2/wal/checkpoint/validation/invariants.rs
@sqlitegraph/src/backend/native/v2/wal/checkpoint/core.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update validate_checkpoint_state_invariants signature</name>
  <files>sqlitegraph/src/backend/native/v2/wal/checkpoint/validation/invariants.rs</files>
  <action>
    Modify the `validate_checkpoint_state_invariants()` method signature:

    1. Change parameter from `&CheckpointState` to accept both `&CheckpointState` and `&CheckpointManagerState`
    2. Import `CheckpointManagerState` from core.rs (add: `use crate::backend::native::v2::wal::checkpoint::core::CheckpointManagerState;`)
    3. Update function signature to: `pub fn validate_checkpoint_state_invariants(&self, state: &CheckpointState, manager_state: &CheckpointManagerState)`

    The CheckpointManagerState struct has the fields needed for validation:
    - current_state: CheckpointState
    - checkpointed_lsn: u64
    - in_progress: bool
    - checkpoint_start_time: Option<Instant>
    - completed_checkpoints: u64
  </action>
  <verify>
    cargo check --lib 2>&1 | head -20
  </verify>
  <done>
    Function signature accepts both CheckpointState enum and CheckpointManagerState struct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement state transition validation</name>
  <files>sqlitegraph/src/backend/native/v2/wal/checkpoint/validation/invariants.rs</files>
  <action>
    Replace the commented-out state invariants (lines 236-278) with proper validation:

    1. Define valid state transitions as a match expression:
       - Idle -> Initializing
       - Initializing -> Collecting
       - Collecting -> Processing
       - Processing -> Flushing
       - Flushing -> Validating
       - Validating -> Complete
       - Any state -> Failed

    2. Add validation that checks:
       - If manager_state.in_progress is true, state should NOT be Idle
       - If manager_state.checkpoint_start_time is Some, state should be past Initializing
       - If state is Complete, manager_state.completed_checkpoints should have increased

    3. Remove all references to non-existent fields (checkpoint_file_path, start_lsn, end_lsn, v2_metadata)
    4. Use V2InvariantViolationType::V2MetadataCorruption for state violations
  </action>
  <verify>
    cargo check --lib 2>&1 | head -20
  </verify>
  <done>
    State transition validation uses actual CheckpointState enum values.
    No references to non-existent struct fields.
    Violations are reported with V2InvariantViolationType.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update comprehensive validation call</name>
  <files>sqlitegraph/src/backend/native/v2/wal/checkpoint/validation/invariants.rs</files>
  <action>
    Update the `validate_comprehensive_v2_invariants()` method (line 399-433):

    1. Add a call to get CheckpointManagerState (requires passing it in or accessing from checkpoint manager)
    2. Update the validate_checkpoint_state_invariants call to pass both state and manager_state
    3. For now, make manager_state an optional parameter since the method signature needs to stay compatible

    Actually, the simplest fix is to keep the method accepting just &CheckpointState but implement internal validation that works with the enum. The state machine validation can be done without CheckpointManagerState.
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -i "error\|warning" | head -20
  </verify>
  <done>
    Comprehensive validation compiles without errors.
    State invariants are validated against enum transitions.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add checkpoint state validation tests</name>
  <files>sqlitegraph/src/backend/native/v2/wal/checkpoint/validation/invariants.rs</files>
  <action>
    Add tests in the tests module to verify state validation works:

    1. test_valid_state_transitions: Create validator, verify valid transitions produce no violations
    2. test_invalid_state_transition: Verify invalid transition (e.g., Idle -> Complete) produces a violation
    3. test_failed_state_from_any: Verify any state can transition to Failed

    Use the existing test pattern from test_checkpoint_state_invariants (line 579-594).
  </action>
  <verify>
    cargo test --lib checkpoint::validation::invariants::tests
  </verify>
  <done>
    Tests verify valid state transitions pass and invalid transitions are detected.
  </done>
</task>

</tasks>

<verification>
1. cargo check --lib passes without errors in checkpoint validation module
2. cargo test --lib checkpoint::validation runs all checkpoint validation tests
3. Commented code in lines 236-278 is replaced with working implementation
</verification>

<success_criteria>
1. validate_checkpoint_state_invariants() is no longer commented out
2. Function works with CheckpointState enum (not assuming struct fields)
3. State transition validation detects invalid transitions
4. All checkpoint validation tests pass
5. No compilation errors related to missing fields
</success_criteria>

<output>
After completion, create `.planning/phases/12-acid-consistency/12-02-SUMMARY.md`
</output>
