---
phase: 12-acid-consistency
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - sqlitegraph/src/backend/native/v2/wal/recovery/core.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Post-recovery validation hook is called after WAL replay completes"
    - "RecoveryValidator validates the replayed transaction sequence"
    - "Validation failures prevent recovery from completing successfully"
    - "Validation warnings are collected and returned to caller"
  artifacts:
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/core.rs"
      provides: "V2WALRecoveryEngine with validate_post_recovery method"
      contains: "validate_post_recovery"
  key_links:
    - from: "attempt_recovery()"
      to: "validate_post_recovery()"
      via: "call after replay_transactions() before finalize_recovery()"
      pattern: "replay_transactions.*validate_post_recovery.*finalize"
    - from: "validate_post_recovery()"
      to: "RecoveryValidator"
      via: "delegate to existing validator"
      pattern: "RecoveryValidator.*validate"
---

<objective>
Add post-recovery validation hook to the WAL recovery path.

Purpose: After WAL replay completes, validate the integrity of the recovered database to ensure no corruption occurred during recovery. The RecoveryValidator already has comprehensive validation infrastructure - we need to integrate it into the recovery completion path.

Output: Database integrity is verified after recovery, with corruption causing recovery failure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-acid-consistency/12-RESEARCH.md
@.planning/STATE.md

@sqlitegraph/src/backend/native/v2/wal/recovery/core.rs
@sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validate_post_recovery method to V2WALRecoveryEngine</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Add a new private method `validate_post_recovery()` to V2WALRecoveryEngine:

    Location: After `finalize_recovery()` method (after line ~400)

    Method signature:
    ```rust
    fn validate_post_recovery(&self, transactions: &[TransactionState]) -> Result<Vec<String>, RecoveryError>
    ```

    Implementation:
    1. Create RecoveryValidator with self.database_path
    2. Call `validator.validate_recovery_sequence(transactions)` to validate all replayed transactions
    3. Return Ok(warnings) if validation succeeds
    4. Return Err(RecoveryError::validation(...)) if validation fails with critical errors

    Import RecoveryValidator:
    ```rust
    use super::validator::RecoveryValidator;
    ```
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -A5 "recovery::core"
  </verify>
  <done>
    validate_post_recovery method exists and uses RecoveryValidator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Call validate_post_recovery in attempt_recovery</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Update the `attempt_recovery()` method (line 259-283) to call post-recovery validation:

    Insert validation call between replay and finalize:
    1. After `replay_transactions(&transactions)?` (line 274)
    2. Before `finalize_recovery()?` (line 278)
    3. Add: `let post_validation_warnings = self.validate_post_recovery(&transactions)?;`
    4. Extend warnings with: `warnings.extend(post_validation_warnings);`

    This ensures integrity is verified before recovery completes.
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -A5 "recovery::core"
  </verify>
  <done>
    attempt_recovery calls validate_post_recovery before finalize_recovery.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add test for post-recovery validation hook</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Add a test in the tests module to verify post-recovery validation hook is called:

    1. test_post_recovery_hook_is_called: Verify validate_post_recovery is invoked during recovery
    2. test_post_recovery_returns_warnings: Verify warnings are collected and returned
    3. test_post_recovery_failure_aborts_recovery: Verify validation failure prevents recovery completion

    Use tempfile for test database creation, following existing test patterns in the recovery module.
  </action>
  <verify>
    cargo test --lib recovery::core::tests::test_post_recovery
  </verify>
  <done>
    Tests verify post-recovery validation hook is called and failures abort recovery.
  </done>
</task>

</tasks>

<verification>
1. cargo check --lib passes without errors in recovery modules
2. cargo test --lib recovery::core::tests runs recovery tests
3. validate_post_recovery is called in attempt_recovery before finalize
</verification>

<success_criteria>
1. validate_post_recovery verifies transaction sequence after WAL replay
2. RecoveryValidator is integrated into recovery path
3. Validation failures prevent recovery from completing successfully
4. Warnings for non-critical issues are collected and returned
5. Tests verify hook is called and failures abort recovery
</success_criteria>

<output>
After completion, create `.planning/phases/12-acid-consistency/12-04-SUMMARY.md`
</output>
