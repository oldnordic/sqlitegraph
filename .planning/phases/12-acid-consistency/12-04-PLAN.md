---
phase: 12-acid-consistency
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - sqlitegraph/src/backend/native/v2/wal/recovery/core.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Post-recovery validation verifies database integrity after WAL replay"
    - "Graph file integrity checks run after transactions are replayed"
    - "Node count consistency is verified between header and replayed data"
    - "Validation failures prevent corrupted database from being used"
  artifacts:
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/core.rs"
      provides: "V2WALRecoveryEngine with post-recovery validation hook"
      contains: "validate_post_recovery"
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs"
      provides: "RecoveryValidator with comprehensive integrity checks"
      min_lines: 50
  key_links:
    - from: "attempt_recovery()"
      to: "validate_post_recovery()"
      via: "call after replay_transactions() before finalize_recovery()"
      pattern: "replay_transactions.*validate_post_recovery.*finalize"
    - from: "validate_post_recovery()"
      to: "RecoveryValidator"
      via: "delegate to existing validator"
      pattern: "RecoveryValidator.*validate"
---

<objective>
Add post-recovery integrity validation to the WAL recovery path.

Purpose: After WAL replay completes, validate the integrity of the recovered database to ensure no corruption occurred during recovery. The RecoveryValidator already has comprehensive validation infrastructure - we need to integrate it into the recovery completion path to verify the database is consistent before marking recovery successful.

Output: Database integrity is verified after recovery, with corruption causing recovery failure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-acid-consistency/12-RESEARCH.md
@.planning/STATE.md

@sqlitegraph/src/backend/native/v2/wal/recovery/core.rs
@sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validate_post_recovery method to V2WALRecoveryEngine</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Add a new private method `validate_post_recovery()` to V2WALRecoveryEngine:

    Location: After `finalize_recovery()` method (after line ~400)

    Method signature:
    ```rust
    fn validate_post_recovery(&self, transactions: &[TransactionState]) -> Result<Vec<String>, RecoveryError>
    ```

    Implementation:
    1. Create RecoveryValidator with self.database_path
    2. Call `validator.validate_recovery_sequence(transactions)` to validate all replayed transactions
    3. Return Ok(warnings) if validation succeeds
    4. Return Err(RecoveryError::validation(...)) if validation fails with critical errors

    Import RecoveryValidator:
    ```rust
    use super::validator::RecoveryValidator;
    ```
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -A5 "recovery::core"
  </verify>
  <done>
    validate_post_recovery method exists and uses RecoveryValidator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add graph file integrity checks to post-recovery validation</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Extend validate_post_recovery to check graph file integrity:

    After transaction validation, add:
    1. Open the graph file at self.database_path
    2. Read the persistent header to get node_count
    3. Verify node_count > 0 if transactions_replayed > 0
    4. Verify file size is reasonable (not truncated)
    5. If options.perform_consistency_checks is true, run additional checks:
       - Verify free space manager consistency
       - Check for orphaned clusters

    Return warnings for non-critical issues, errors for corruption.
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -A5 "recovery::core"
  </verify>
  <done>
    Post-recovery validation checks graph file integrity.
  </done>
</task>

<task type="auto">
  <name>Task 3: Call validate_post_recovery in attempt_recovery</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Update the `attempt_recovery()` method (line 259-283) to call post-recovery validation:

    Insert validation call between replay and finalize:
    1. After `replay_transactions(&transactions)?` (line 274)
    2. Before `finalize_recovery()?` (line 278)
    3. Add: `let post_validation_warnings = self.validate_post_recovery(&transactions)?;`
    4. Extend warnings with: `warnings.extend(post_validation_warnings);`

    This ensures integrity is verified before recovery completes.
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -A5 "recovery::core"
  </verify>
  <done>
    attempt_recovery calls validate_post_recovery before finalize_recovery.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add test for post-recovery validation</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Add a test in the tests module to verify post-recovery validation:

    1. test_post_recovery_validation_passes: Create valid WAL with committed transaction, recover successfully
    2. test_post_recovery_validation_catches_corruption: Create graph file with inconsistent node_count, verify recovery fails
    3. test_post_recovery_with_empty_transactions: Verify recovery succeeds when WAL is empty (no transactions replayed)

    Use tempfile for test database creation, following existing test patterns in the validator module tests.
  </action>
  <verify>
    cargo test --lib recovery::core::tests::test_post_recovery
  </verify>
  <done>
    Tests verify post-recovery validation catches corruption and allows clean recovery.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add RecoveryValidator::validate_database_integrity method</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs</files>
  <action>
    Add a method to RecoveryValidator for database-level integrity checks:

    Method signature:
    ```rust
    pub fn validate_database_integrity(&self) -> RecoveryResult<ValidationResult>
    ```

    Implementation:
    1. Open graph file from self.graph_file_path
    2. Read and validate persistent header
    3. Check node count matches actual nodes in store
    4. Verify free space manager consistency
    5. Check for cluster alignment violations
    6. Return ValidationResult with any issues found

    This provides a reusable integrity check that can be called from recovery core.
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -A5 "recovery::validator"
  </verify>
  <done>
    validate_database_integrity provides comprehensive database integrity checks.
  </done>
</task>

</tasks>

<verification>
1. cargo check --lib passes without errors in recovery modules
2. cargo test --lib recovery::core::tests runs recovery tests
3. cargo test --lib recovery::validator::tests runs validator tests
4. validate_post_recovery is called in attempt_recovery before finalize
</verification>

<success_criteria>
1. validate_post_recovery verifies database integrity after WAL replay
2. Graph file header consistency is checked (node_count, file_size)
3. RecoveryValidator::validate_database_integrity provides comprehensive checks
4. Validation failures prevent recovery from completing successfully
5. Tests verify both successful recovery and corruption detection
6. Warnings for non-critical issues are collected and returned
</success_criteria>

<output>
After completion, create `.planning/phases/12-acid-consistency/12-04-SUMMARY.md`
</output>
