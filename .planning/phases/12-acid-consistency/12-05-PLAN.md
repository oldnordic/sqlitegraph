---
phase: 12-acid-consistency
plan: 05
type: execute
wave: 3
depends_on: ["12-04"]
files_modified:
  - sqlitegraph/src/backend/native/v2/wal/recovery/core.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Graph file integrity checks verify database consistency after recovery"
    - "Node count consistency is verified between header and replayed data"
    - "Free space manager consistency is checked"
    - "Database-level integrity validation is reusable"
  artifacts:
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/core.rs"
      provides: "Extended post-recovery validation with graph file checks"
      contains: "graph file integrity"
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs"
      provides: "RecoveryValidator with validate_database_integrity method"
      min_lines: 50
  key_links:
    - from: "validate_post_recovery()"
      to: "validate_database_integrity()"
      via: "call after transaction validation"
      pattern: "validate_recovery_sequence.*validate_database_integrity"
    - from: "validate_database_integrity()"
      to: "graph file"
      via: "open and read persistent header"
      pattern: "GraphFile.*persistent_header"
---

<objective>
Add comprehensive database integrity checks to post-recovery validation.

Purpose: Extend the post-recovery validation hook to check graph file integrity, including node count consistency, file size validation, and free space manager checks. Also add a reusable validate_database_integrity method to RecoveryValidator.

Output: Comprehensive integrity checks verify database consistency after recovery.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-acid-consistency/12-RESEARCH.md
@.planning/phases/12-acid-consistency/12-04-SUMMARY.md
@.planning/STATE.md

@sqlitegraph/src/backend/native/v2/wal/recovery/core.rs
@sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RecoveryValidator::validate_database_integrity method</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs</files>
  <action>
    Add a method to RecoveryValidator for database-level integrity checks:

    Method signature:
    ```rust
    pub fn validate_database_integrity(&self) -> RecoveryResult<ValidationResult>
    ```

    Implementation:
    1. Open graph file from self.graph_file_path
    2. Read and validate persistent header
    3. Check node count matches actual nodes in store
    4. Verify free space manager consistency
    5. Check for cluster alignment violations
    6. Return ValidationResult with any issues found

    This provides a reusable integrity check that can be called from recovery core.
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -A5 "recovery::validator"
  </verify>
  <done>
    validate_database_integrity provides comprehensive database integrity checks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend validate_post_recovery with graph file checks</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Extend validate_post_recovery to check graph file integrity:

    After transaction validation, add:
    1. Open the graph file at self.database_path
    2. Read the persistent header to get node_count
    3. Verify node_count > 0 if transactions_replayed > 0
    4. Verify file size is reasonable (not truncated)
    5. If options.perform_consistency_checks is true, call validator.validate_database_integrity()

    Return warnings for non-critical issues, errors for corruption.
  </action>
  <verify>
    cargo check --lib 2>&1 | grep -A5 "recovery::core"
  </verify>
  <done>
    Post-recovery validation checks graph file integrity.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add comprehensive integrity check tests</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/core.rs</files>
  <action>
    Add tests to verify comprehensive integrity checks:

    1. test_post_recovery_validates_node_count: Create graph with inconsistent node_count, verify recovery fails
    2. test_post_recovery_detects_truncated_file: Create truncated graph file, verify recovery fails
    3. test_post_recovery_validates_free_space: Verify free space manager consistency is checked
    4. test_post_recovery_with_empty_wal: Verify recovery succeeds when WAL is empty (no transactions replayed)

    Use tempfile for test database creation, following existing test patterns.
  </action>
  <verify>
    cargo test --lib recovery::core::tests::test_post_recovery
  </verify>
  <done>
    Tests verify comprehensive integrity checks catch corruption and allow clean recovery.
  </done>
</task>

</tasks>

<verification>
1. cargo check --lib passes without errors in recovery modules
2. cargo test --lib recovery::core::tests runs recovery tests
3. cargo test --lib recovery::validator::tests runs validator tests
4. validate_database_integrity is called from validate_post_recovery
</verification>

<success_criteria>
1. validate_database_integrity provides comprehensive database-level checks
2. Graph file header consistency is checked (node_count, file_size)
3. Free space manager consistency is verified
4. Validation failures prevent recovery from completing successfully
5. Tests verify both successful recovery and corruption detection
</success_criteria>

<output>
After completion, create `.planning/phases/12-acid-consistency/12-05-SUMMARY.md`
</output>
