---
phase: 21-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations_with_problematic_tests.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/mod.rs
autonomous: true

must_haves:
  truths:
    - "Node deletion rollback tests pass without 'will fail until implementation complete' markers"
    - "All 8 node deletion rollback tests use real implementation, not mock"
    - "Tests verify before-image data (node record + edges) is captured"
    - "Tests verify rollback restores node with all edges"
  artifacts:
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations_with_problematic_tests.rs"
      provides: "TDD tests for node deletion rollback"
      min_lines: 800
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations/node_ops.rs"
      provides: "Real node deletion rollback implementation"
      contains: "handle_node_delete"
  key_links:
    - from: "operations_with_problematic_tests.rs tests"
      to: "operations/node_ops.rs:handle_node_delete"
      via: "mod.rs replayer uses real operations module"
      pattern: "handle_node_delete.*real.*implementation"
---

<objective>
Enable and fix WAL recovery tests for node deletion rollback (TEST-WAL-01 through TEST-WAL-04)

Purpose: The 8 tests in `operations_with_problematic_tests.rs` (lines 831-1080) are marked with "TODO: This test will fail until real implementation is complete". These tests currently use a mock implementation that only logs a warning. The real implementation exists in `operations/node_ops.rs` but is not being used by these tests.
Output: All 8 node deletion rollback tests pass, using the real implementation from `operations/node_ops.rs`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations_with_problematic_tests.rs
@sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations/node_ops.rs
@sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/node_ops.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire real handle_node_delete implementation to tests</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations_with_problematic_tests.rs</files>
  <action>
    Remove the mock `handle_node_delete` implementation from DefaultReplayOperations (lines 453-463 in operations_with_problematic_tests.rs).

    The mock currently just logs "Node delete replay not yet implemented - placeholder" and returns Ok(()).

    Instead, delegate to the real implementation from operations/node_ops.rs which:
    1. Validates input parameters (node_id != 0)
    2. Deserializes NodeRecordV2 from old_data using binary deserialization
    3. Captures outgoing and incoming edges BEFORE deletion (lines 172-214 in node_ops.rs)
    4. Performs edge cascade cleanup using EdgeStore (lines 216-272)
    5. Cleans up cluster references and deallocates using FreeSpaceManager (lines 274-313)
    6. Deletes node from NodeStore (line 315)
    7. Records RollbackOperation::NodeDelete with captured data (lines 323-329)

    Pattern to follow: The `handle_node_update` method in DefaultReplayOperations (lines ~400-450 of operations_with_problematic_tests.rs) shows how to implement a replay operation properly - use the graph_file lock, create stores, and record rollback data.

    DO NOT modify operations/node_ops.rs - it already has the correct implementation. Just wire the tests to use it OR inline the same logic into DefaultReplayOperations.
  </action>
  <verify>cargo test -p sqlitegraph --lib backend::native::v2::wal::recovery::replayer::operations_with_problematic_tests::tests::test_handle_node_delete_basic</verify>
  <done>test_handle_node_delete_basic passes with real implementation (not mock)</done>
</task>

<task type="auto">
  <name>Task 2: Remove TODO markers and verify all 8 tests pass</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations_with_problematic_tests.rs</files>
  <action>
    After Task 1 wires the real implementation, remove all "TODO: This test will fail until real implementation is complete" comments from the 8 tests:

    1. test_handle_node_delete_basic (line 843)
    2. test_handle_node_delete_with_old_data (line 881)
    3. test_handle_node_delete_nonexistent_node (line 909)
    4. test_handle_node_delete_with_cluster_references (line 947)
    5. test_handle_node_delete_malformed_old_data (line 981)
    6. test_handle_node_delete_zero_node_id (line 1003)
    7. test_handle_node_delete_rollback_operation_preserves_slot_offset (line 1027)
    8. test_handle_node_delete_edge_cleanup_required (line 1069)

    Replace TODO comments with assertions that verify the correct behavior:

    - For tests with old_data: verify old_data is preserved in rollback operation
    - For tests with cluster_references: verify edges are captured before deletion
    - For edge cleanup tests: verify edges are deleted AND captured for rollback

    Run all 8 tests and ensure they pass.
  </action>
  <verify>cargo test -p sqlitegraph --lib -- test_handle_node_delete 2>&1 | grep -E "test result|passed|failed"</verify>
  <done>8/8 node deletion tests pass, no TODO markers remain</done>
</task>

<task type="auto">
  <name>Task 3: Verify rollback_node_delete restores nodes correctly</name>
  <files>sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/node_ops.rs</files>
  <action>
    Add an integration test that verifies the full rollback cycle:

    1. Create a node with edges (record state before)
    2. Call handle_node_delete to delete it (verify deletion)
    3. Call rollback_node_delete with the captured rollback data
    4. Verify node is restored with correct data
    5. Verify edges are restored

    The rollback_node_delete function already exists (rollback/node_ops.rs lines 138-436) and:
    - Deserializes old node data
    - Writes node back to NodeStore
    - Restores outgoing cluster from captured edges (lines 196-296)
    - Restores incoming cluster from captured edges (lines 298-397)
    - Reclaims slot from free list (lines 399-430)

    Add test to operations_with_problematic_tests.rs that exercises this full cycle.

    Use this test structure:
    ```rust
    #[test]
    fn test_full_node_delete_and_restore_cycle() {
        // Create node with edges
        // Delete it (capture rollback data)
        // Verify deleted
        // Rollback using captured data
        // Verify restored with edges
    }
    ```
  </action>
  <verify>cargo test -p sqlitegraph --lib test_full_node_delete_and_restore_cycle</verify>
  <done>Full delete/rollback cycle test passes, node and edges restored correctly</done>
</task>

</tasks>

<verification>
Overall phase checks:

1. Run all WAL recovery tests:
   ```bash
   cargo test -p sqlitegraph --lib -- test_handle_node_delete
   ```
   Expected: All 8 tests pass

2. Verify no TODO markers remain:
   ```bash
   grep -n "TODO.*will fail" sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations_with_problematic_tests.rs
   ```
   Expected: No results

3. Verify tests use real implementation:
   ```bash
   grep -A5 "fn handle_node_delete" sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations_with_problematic_tests.rs
   ```
   Expected: Implementation does more than just logging a warning
</verification>

<success_criteria>
1. All 8 node deletion rollback tests pass
2. No "TODO: will fail until implementation complete" comments remain
3. Tests verify edge capture before deletion
4. Tests verify rollback restores nodes with all edges
5. Full delete/rollback cycle test passes
</success_criteria>

<output>
After completion, create `.planning/phases/21-test-coverage/21-01-SUMMARY.md`
</output>
