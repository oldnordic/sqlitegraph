---
phase: 13-acid-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sqlitegraph/src/backend/native/v2/wal/transaction_coordinator.rs
  - sqlitegraph/src/backend/native/v2/wal/manager.rs
  - sqlitegraph/src/backend/native/v2/wal/mod.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - Tests run without tokio runtime dependency
    - Transaction API uses consistent IsolationLevel type across coordinator and manager
    - Transaction coordinator can begin, commit, and rollback transactions
    - Lock manager can acquire and release locks
  artifacts:
    - path: sqlitegraph/src/backend/native/v2/wal/transaction_coordinator.rs
      provides: "Synchronous transaction coordination with lock management"
      contains: "pub fn begin_transaction", "pub fn acquire_lock", "pub fn commit_transaction"
    - path: sqlitegraph/src/backend/native/v2/wal/manager.rs
      provides: "WAL manager using unified IsolationLevel enum"
      contains: "TransactionIsolation replaced with IsolationLevel"
    - path: sqlitegraph/src/backend/native/v2/wal/mod.rs
      provides: "Public exports for IsolationLevel, TransactionContext, ResourceId"
      exports: ["IsolationLevel", "TransactionContext", "ResourceId", "LockType"]
  key_links:
    - from: "transaction_coordinator.rs"
      to: "manager.rs"
      via: "Use unified IsolationLevel enum instead of TransactionIsolation"
      pattern: "IsolationLevel::"
    - from: "V2TransactionCoordinator::begin_transaction"
      to: "V2LockManager::acquire_lock"
      via: "Synchronous lock acquisition for resource isolation"
      pattern: "lock_manager\\.acquire_lock\\("
---

<objective>
Convert transaction coordinator from async to synchronous code and unify isolation level enums across the codebase.

Purpose: The current transaction coordinator uses async/await but no tokio runtime exists (all tests are #[ignore] with note about "requires tokio runtime"). Converting to sync enables immediate testing and use. Additionally, two different isolation level enums exist (IsolationLevel in coordinator, TransactionIsolation in manager) causing confusion.

Output: Synchronous transaction coordinator with unified IsolationLevel enum, enabling tests to run and preparing for wait-for graph integration in 13-02.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-acid-isolation/13-RESEARCH.md
@.planning/phases/13-acid-isolation/13-CONTEXT.md
@sqlitegraph/src/backend/native/v2/wal/transaction_coordinator.rs
@sqlitegraph/src/backend/native/v2/wal/manager.rs
@sqlitegraph/src/backend/native/types/errors.rs
</context>

<tasks>

<task type="auto">
  <name>Convert all async methods in transaction_coordinator.rs to synchronous</name>
  <files>sqlitegraph/src/backend/native/v2/wal/transaction_coordinator.rs</files>
  <action>
    For each method in transaction_coordinator.rs:
    1. Remove `async` keyword from function signature
    2. Remove `.await` from all calls within the method
    3. Return futures as direct values

    Specific methods to convert (each has `pub async fn`):
    - V2LockManager::acquire_lock() → pub fn acquire_lock()
    - V2LockManager::release_lock() → pub fn release_lock()
    - V2LockManager::add_to_wait_queue() → pub fn add_to_wait_queue()
    - V2LockManager::process_wait_queue() → pub fn process_wait_queue()
    - DeadlockDetector::would_cause_deadlock() → pub fn would_cause_deadlock()
    - DeadlockDetector::remove_transaction() → pub fn remove_transaction()
    - IsolationManager::validate_access() → pub fn validate_access()
    - V2TransactionCoordinator::begin_transaction() → pub fn begin_transaction()
    - V2TransactionCoordinator::acquire_lock() → pub fn acquire_lock()
    - V2TransactionCoordinator::release_lock() → pub fn release_lock()
    - V2TransactionCoordinator::commit_transaction() → pub fn commit_transaction()
    - V2TransactionCoordinator::rollback_transaction() → pub fn rollback_transaction()
    - V2TransactionCoordinator::create_savepoint() → pub fn create_savepoint()
    - V2TransactionCoordinator::rollback_to_savepoint() → pub fn rollback_to_savepoint()
    - V2TransactionCoordinator::get_transaction_status() → pub fn get_transaction_status()
    - V2TransactionCoordinator::list_active_transactions() → pub fn list_active_transactions()
    - V2TransactionCoordinator::cleanup_abandoned_transactions() → pub fn cleanup_abandoned_transactions()
    - TwoPhaseCommitCoordinator::commit_transaction() → pub fn commit_transaction()
    - TwoPhaseCommitCoordinator::prepare_transaction() → pub fn prepare_transaction()
    - TwoPhaseCommitCoordinator::finalize_commit() → pub fn finalize_commit()
    - TwoPhaseCommitCoordinator::abort_transaction() → pub fn abort_transaction()
    - TwoPhaseCommitCoordinator::validate_commit_resources() → pub fn validate_commit_resources()
    - TwoPhaseCommitCoordinator::validate_pre_commit() → pub fn validate_pre_commit()

    Do NOT modify test functions (they will be updated in later plans).
  </action>
  <verify>grep -n "async fn\|\.await" sqlitegraph/src/backend/native/v2/wal/transaction_coordinator.rs | grep -v "test_disabled\|requires tokio" | wc -l returns 0</verify>
  <done>All non-test methods are synchronous (no async/await), grep shows no remaining async fn or .await</done>
</task>

<task type="auto">
  <name>Unify TransactionIsolation and IsolationLevel enums</name>
  <files>sqlitegraph/src/backend/native/v2/wal/manager.rs, sqlitegraph/src/backend/native/v2/wal/transaction_coordinator.rs</files>
  <action>
    1. In manager.rs, replace TransactionIsolation enum with IsolationLevel from transaction_coordinator:
       - Remove: `pub enum TransactionIsolation { ReadCommitted, Serializable, Snapshot }`
       - Replace all `TransactionIsolation` with `IsolationLevel`
       - Update begin_transaction signature: `pub fn begin_transaction(&self, isolation_level: IsolationLevel)`

    2. For Snapshot isolation level (in manager.rs but not in coordinator):
       - Add `Snapshot = 4` variant to IsolationLevel enum in transaction_coordinator.rs
       - Or handle Snapshot by mapping to Serializable (decision point: add Snapshot variant)

    3. Update all references in manager.rs tests:
       - `TransactionIsolation::ReadCommitted` → `IsolationLevel::ReadCommitted`
       - `TransactionIsolation::Serializable` → `IsolationLevel::Serializable`
       - `TransactionIsolation::Snapshot` → `IsolationLevel::Snapshot` (if added)

    4. Verify no orphan TransactionIsolation references remain:
       - `:g/TTransactionIsolation` should find zero matches
  </action>
  <verify>grep -rn "TransactionIsolation" sqlitegraph/src/backend/native/v2/wal/manager.rs returns 0 (or only in comments)</verify>
  <done>Single IsolationLevel enum used across both files, all tests compile with unified enum</done>
</task>

<task type="auto">
  <name>Enable previously ignored tests and verify sync conversion</name>
  <files>sqlitegraph/src/backend/native/v2/wal/transaction_coordinator.rs</files>
  <action>
    1. Remove #[ignore] attributes from test functions:
       - test_transaction_coordinator_basic (line ~1223)
       - test_savepoint_rollback (line ~1231)
       - test_pre_commit_rejects_invalid_node_id (line ~1239)
       - test_pre_commit_rejects_invalid_cluster_offset (line ~1251)
       - test_pre_commit_accepts_valid_records (line ~1263)

    2. Update test implementations to be synchronous:
       - Remove any .await calls from test code
       - Remove tokio::test wrapper (not present since tests are #[ignore])

    3. Run the tests to verify they compile and run:
       - `cargo test -p sqlitegraph transaction_coordinator --lib`
  </action>
  <verify>cargo test -p sqlitegraph transaction_coordinator --lib passes all 5 previously-ignored tests</verify>
  <done>Tests run without tokio runtime, demonstrating successful sync conversion</done>
</task>

</tasks>

<verification>
Overall phase verification:
1. No async/await remains in transaction_coordinator.rs non-test code
2. Single IsolationLevel enum used across manager.rs and transaction_coordinator.rs
3. Tests run without tokio dependency
4. `cargo check -p sqlitegraph` passes without warnings
</verification>

<success_criteria>
1. All transaction coordinator methods are synchronous (no async/await)
2. TransactionIsolation enum eliminated, replaced with IsolationLevel
3. At least 5 previously-ignored tests now pass
4. Code compiles with no tokio requirement
</success_criteria>

<output>
After completion, create `.planning/phases/13-acid-isolation/13-01-SUMMARY.md`
</output>
