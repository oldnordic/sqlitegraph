---
phase: 10-testing-and-docs
plan: 01
type: execute
depends_on: []
files_modified: [sqlitegraph/tests/wal_core_tests.rs]
autonomous: true
---

<objective>
Fix broken WAL tests and add missing WAL recovery edge case tests.

Purpose: Phase 2 WAL integration left test files in a non-compiling state. This plan fixes those tests and adds comprehensive edge case coverage for WAL recovery.
Output: All WAL tests compiling and passing with additional edge case coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# WAL configuration changes from Phase 2
@sqlitegraph/src/backend/native/v2/wal/mod.rs
@sqlitegraph/tests/wal_core_tests.rs

# Existing test patterns
@sqlitegraph/tests/mvcc_edge_case_tests.rs
@sqlitegraph/tests/mvcc_wal_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix V2WALConfig struct literals in wal_core_tests.rs</name>
  <files>sqlitegraph/tests/wal_core_tests.rs</files>
  <action>
The V2WALConfig struct has these fields:
- graph_path: PathBuf
- wal_path: PathBuf
- checkpoint_path: PathBuf
- max_wal_size: u64
- buffer_size: usize
- checkpoint_interval: u64
- group_commit_timeout_ms: u64
- max_group_commit_size: usize
- enable_compression: bool
- compression_level: u8
- auto_checkpoint: bool (ADDED IN PHASE 02-01)
- background_checkpoint_interval_secs: u64 (ADDED IN PHASE 02-01)
- background_checkpoint_thread: Option<JoinHandle<()>> (ADDED IN PHASE 02-01)
- parallel_recovery: bool (ADDED IN PHASE 07-01)
- max_parallel_transactions: usize (ADDED IN PHASE 07-01)

Fix all V2WALConfig struct literals in wal_core_tests.rs (lines 248, 306, 364) to include these missing fields with appropriate test values:
- auto_checkpoint: false (for manual checkpoint control in tests)
- background_checkpoint_interval_secs: 0 (disabled for tests)
- background_checkpoint_thread: None
- parallel_recovery: false (keep tests sequential unless testing parallelism)
- max_parallel_transactions: 4

Use ..Default::default() for fields with reasonable defaults where appropriate.
  </action>
  <verify>cargo test --test wal_core_tests compiles without errors</verify>
  <done>All V2WALConfig literals compile and tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add WAL recovery edge case tests</name>
  <files>sqlitegraph/tests/wal_recovery_edge_cases.rs</files>
  <action>
Add comprehensive WAL recovery edge case tests. The file exists - extend it with these scenarios:

1. **Corrupted WAL header scenarios** (4 tests)
   - Invalid magic bytes
   - Version mismatch
   - Truncated header
   - Checksum validation failure

2. **Transaction edge cases** (4 tests)
   - Empty transaction (no records)
   - Single transaction overflow
   - Transaction with only rollback markers
   - Partial transaction at end of WAL

3. **Checkpoint edge cases** (4 tests)
   - Checkpoint during active transaction
   - Multiple checkpoints in sequence
   - Checkpoint with empty WAL
   - Checkpoint recovery with missing checkpoint file

4. **Recovery scenarios** (4 tests)
   - Recovery after crash mid-transaction
   - Recovery with corrupted records (skip and continue)
   - Recovery from empty WAL
   - Recovery with mismatched LSN sequence

Each test should:
- Create test fixture with temp files
- Execute the edge case scenario
- Verify correct recovery behavior
- Clean up temp files

Follow existing test patterns from mvcc_wal_tests.rs and wal_core_tests.rs.
  </action>
  <verify>cargo test --test wal_recovery_edge_cases -- --nocapture passes all 16 new tests</verify>
  <done>16 WAL recovery edge case tests added and passing</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify no regressions</name>
  <files></files>
  <action>
Run the complete test suite to ensure:
1. All WAL-related tests pass
2. No new compilation errors
3. No test regressions in other modules
4. Fix any issues discovered

If tests fail:
- Investigate root cause
- Fix the issue (test or implementation)
- Re-run until clean

Commands to run:
- cargo test --lib (unit tests)
- cargo test --test wal_core_tests
- cargo test --test wal_recovery_edge_cases
- cargo test --test mvcc_wal_tests
  </action>
  <verify>All tests pass with 100% success rate</verify>
  <done>Full test suite passes without errors or warnings</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --test wal_core_tests compiles and passes
- [ ] 16 new WAL recovery edge case tests added and passing
- [ ] No test regressions in other modules
- [ ] cargo check --all-targets produces zero errors
</verification>

<success_criteria>

- All V2WALConfig struct literals compile
- 16 WAL recovery edge case tests passing
- Full test suite passes (zero test failures)
- No compilation errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/10-testing-and-docs/10-01-SUMMARY.md`
</output>
