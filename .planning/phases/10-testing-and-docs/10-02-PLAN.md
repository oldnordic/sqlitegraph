---
phase: 10-testing-and-docs
plan: 02
type: execute
depends_on: ["01"]
files_modified: [sqlitegraph/tests/mvcc_concurrent_tests.rs, sqlitegraph/src/algo.rs]
autonomous: true
---

<objective>
Add comprehensive concurrent operation tests for algorithms and edge cases.

Purpose: Phase 4 added MVCC concurrent tests but algorithm concurrent operations and additional edge cases need coverage. This plan adds stress testing for concurrent operations.
Output: Comprehensive concurrent operation test suite passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing concurrent test patterns
@sqlitegraph/tests/mvcc_concurrent_tests.rs
@sqlitegraph/src/mvcc/snapshot.rs
@sqlitegraph/src/algo.rs

# Algorithm modules to test concurrently
@sqlitegraph/src/algo.rs
@sqlitegraph/src/bfs.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend mvcc_concurrent_tests.rs with algorithm stress tests</name>
  <files>sqlitegraph/tests/mvcc_concurrent_tests.rs</files>
  <action>
Add concurrent stress tests for graph algorithms. The existing file has 16 tests - add more:

1. **Concurrent algorithm execution** (4 tests)
   - Multiple threads running BFS from different start nodes
   - Concurrent PageRank with shared graph reads
   - Parallel k-hop queries with overlapping neighborhoods
   - Concurrent shortest path computations

2. **Snapshot isolation during algorithms** (3 tests)
   - Algorithm sees consistent snapshot despite concurrent writes
   - Long-running algorithm (PageRank) with background writes
   - Multiple snapshots taken during algorithm execution

3. **Stress test patterns** (3 tests)
   - 100 threads: 50 readers + 50 writers
   - 1000 operations: alternating reads/writes
   - Rapid snapshot creation/destruction (10K iterations)

Each test should:
- Use rayon for thread pool management
- Verify no data races (compile with miri if possible)
- Assert correct results under concurrent load
- Use Arc<SqliteGraph> for shared access

Follow existing concurrent test patterns in mvcc_concurrent_tests.rs.
  </action>
  <verify>cargo test --test mvcc_concurrent_tests passes all new tests</verify>
  <done>10 new concurrent tests added and passing</done>
</task>

<task type="auto">
  <name>Task 2: Add concurrent algorithm tests in algo.rs module</name>
  <files>sqlitegraph/src/algo.rs</files>
  <action>
Add unit tests directly in algo.rs for concurrent algorithm scenarios:

1. **Thread safety verification** (3 tests)
   - Verify all algorithms are Send + Sync
   - Concurrent PageRank calls with different parameters
   - Concurrent betweenness centrality on disjoint graphs

2. **Algorithm consistency** (2 tests)
   - Same algorithm produces same result across multiple threads
   - Deterministic output under concurrent access

Add these tests to the existing #[cfg(test)] module in algo.rs.

Note: SqliteGraph itself is NOT thread-safe for writes, but read-only algorithms can be called from multiple threads if each has its own graph connection or snapshot.
  </action>
  <verify>cargo test --lib algo::tests passes all new concurrent tests</verify>
  <done>5 algorithm concurrent unit tests added and passing</done>
</task>

<task type="auto">
  <name>Task 3: Add lifecycle edge case tests</name>
  <files>sqlitegraph/tests/mvcc_edge_case_tests.rs</files>
  <action>
Extend mvcc_edge_case_tests.rs with additional edge cases:

1. **Snapshot lifecycle edge cases** (4 tests)
   - Snapshot outlives graph (should work with Arc)
   - Dropped snapshot access (should error gracefully)
   - Nested snapshots (snapshot of snapshot)
   - Snapshot during rollback

2. **Transaction edge cases** (3 tests)
   - Empty transaction (commit with no operations)
   - Rollback after partial commit
   - Transaction with only failed operations

Each test should verify correct behavior and appropriate error handling.
  </action>
  <verify>cargo test --test mvcc_edge_case_tests passes all new tests</verify>
  <done>7 new lifecycle edge case tests added and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All concurrent tests pass (mvcc_concurrent_tests)
- [ ] Algorithm concurrent unit tests pass
- [ ] Edge case tests pass (mvcc_edge_case_tests)
- [ ] No deadlocks or data races detected
</verification>

<success_criteria>

- 15 new concurrent/edge case tests passing
- All existing tests still pass (no regressions)
- Thread safety verified for read-only algorithms
- Comprehensive edge case coverage for snapshots and transactions
  </success_criteria>

<output>
After completion, create `.planning/phases/10-testing-and-docs/10-02-SUMMARY.md`
</output>
