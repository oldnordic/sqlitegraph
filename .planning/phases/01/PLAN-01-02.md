# Plan 01-02: Remove Unused Imports and Dead Code

**Status:** Pending
**Type:** Cleanup
**Research:** None required
**Dependencies:** PLAN-01-01 (complete restructure first)
**Blocked by:** PLAN-01-01
**Blocks:** PLAN-01-03

## Summary

Clean up unused imports and dead code identified by clippy throughout the WAL recovery modules and other codebase areas. This reduces code clutter and improves compilation times.

## Files to Modify

Based on clippy output:

1. `sqlitegraph/src/backend/sqlite/impl_.rs:262` - Remove `use std::fs;`
2. `sqlitegraph/src/backend/native/graph_file/memory_resource_manager/mod.rs:10` - Remove `use memmap2::MmapMut;`
3. `sqlitegraph/src/backend/native/graph_file/memory_resource_manager/operations.rs:18` - Remove `use memmap2::MmapMut;`
4. `sqlitegraph/src/backend/native/graph_backend.rs:85` - Remove `use std::path::Path;`
5. `sqlitegraph/src/backend/native/v2/wal/recovery/replayer/types.rs:8` - Remove `use std::path::PathBuf;`
6. `sqlitegraph/src/backend/native/v2/wal/recovery/replayer/operations.rs:7-21` - Remove multiple unused imports
7. `sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback.rs:6` - Remove `use NativeResult;`
8. `sqlitegraph/src/backend/native/v2/wal/recovery/replayer/mod.rs:17-19` - Remove multiple unused imports

## Tasks

| Task | Description | Files |
|------|-------------|-------|
| 01-02a | Clean operations.rs unused imports | operations.rs |
| 01-02b | Clean rollback.rs unused imports | rollback.rs |
| 01-02c | Clean replayer/mod.rs unused imports | mod.rs |
| 01-02d | Clean other files with unused imports | types.rs, impl_.rs, etc. |

## Verification

```bash
# Run clippy to verify all unused import warnings are gone
cargo clippy -- -W unused_imports

# Should output: "Finished `dev` profile [unoptimized + debuginfo] check(s) in X.XXs"
# with no unused_import warnings
```

## Success Criteria

- [ ] All `unused_import` warnings resolved
- [ ] All `unused_macros` warnings resolved
- [ ] No behavior changes (tests still pass)
- [ ] `cargo clippy` clean on WAL modules

## Notes

- Use auto-fix: `cargo clippy --fix --allow-dirty --allow-staged`
- Verify each change doesn't break functionality
- Commit after each file for safety
