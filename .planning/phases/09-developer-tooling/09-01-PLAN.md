---
phase: 09-developer-tooling
plan: 01
type: execute
depends_on: []
files_modified: [sqlitegraph/src/lib.rs, sqlitegraph/src/introspection.rs, sqlitegraph/src/graph/mod.rs]
autonomous: true
---

<objective>
Add introspection APIs for debugging and LLM feedback.

Purpose: Enable developers and AI agents to inspect internal graph state for debugging and observability.
Output: New introspection module with GraphIntrospection trait and public API methods.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-developer-tooling/09-DISCOVERY.md
@/home/feanor/Projects/sqlitegraph/sqlitegraph/src/lib.rs
@/home/feanor/Projects/sqlitegraph/sqlitegraph/src/cache.rs
@/home/feanor/Projects/sqlitegraph/sqlitegraph/src/graph/mod.rs
@/home/feanor/Projects/sqlitegraph/sqlitegraph/src/graph/metrics/metrics_core.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create introspection module with GraphIntrospection struct</name>
  <files>sqlitegraph/src/introspection.rs</files>
  <action>Create src/introspection.rs module with:

1. GraphIntrospection struct with JSON-serializable fields:
   - backend_type: String ("sqlite" or "native-v2")
   - node_count: usize
   - edge_count: usize (estimated or exact)
   - cache_stats: CacheStats (hits, misses, entries, hit_ratio)
   - memory_usage: Option<usize> (if available)
   - file_size: Option<u64> (for file-based backends)
   - wal_size: Option<u64> (for native-v2 with WAL)

2. IntrospectError enum for introspection-specific failures

3. Implement serde::Serialize for GraphIntrospection (LLM consumable)

DO NOT include sensitive data (passwords, keys).
DO handle cases where counts are unavailable (use None).</action>
  <verify>cargo check passes with new module
cargo doc generates without errors</verify>
  <done>introspection.rs compiles, GraphIntrospection is serializable</done>
</task>

<task type="auto">
  <name>Task 2: Add introspection methods to SqliteGraph</name>
  <files>sqlitegraph/src/graph/mod.rs</files>
  <action>Add introspection methods to SqliteGraph:

1. pub fn introspect(&self) -> Result<GraphIntrospection, SqliteGraphError>
   - Collect backend type
   - Count nodes via all_entity_ids()
   - Estimate edges (sample or backend-specific query)
   - Get cache stats
   - Get file size if applicable

2. pub fn cache_stats(&self) -> CacheStats
   - Return adjacency cache statistics
   - Calculate hit ratio: hits / (hits + misses)

3. Add use crate::introspection::GraphIntrospection

For Native V2: access backend.file_size() if available
For SQLite: query sqlite_master for table stats

DO NOT count edges exhaustively on large graphs (O(E) is too expensive).
DO use sampling or backend metadata for edge estimates.</action>
  <verify>cargo test passes with introspection methods
Manual test: in-memory graph returns introspection without file_size</verify>
  <done>introspect() and cache_stats() methods work on both backends</done>
</task>

<task type="auto">
  <name>Task 3: Export introspection API in lib.rs</name>
  <files>sqlitegraph/src/lib.rs</files>
  <action>Add public module and re-exports:

1. Add pub mod introspection;
2. Add to documentation:
   - Introspection utilities section under "Utilities"
3. Re-export: pub use introspection::{GraphIntrospection, IntrospectError};

Update module docs to include introspection example.

DO NOT export internal implementation details.</action>
  <verify>cargo doc --no-deps shows introspection in public API
cargo test --doc introspection passes</verify>
  <done>GraphIntrospection visible in public API docs</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes (all targets)
- [ ] cargo test passes (including new introspection tests)
- [ ] GraphIntrospection serializes to JSON correctly
- [ ] Both backends (SQLite and Native V2) return valid introspection data
- [ ] cargo doc --no-deps includes introspection documentation
</verification>

<success_criteria>

- introspection.rs module created with GraphIntrospection struct
- SqliteGraph has introspect() and cache_stats() methods
- Public API exported via lib.rs
- JSON serialization works for LLM consumption
- Tests cover both backends
- Documentation examples are clear

</success_criteria>

<output>
After completion, create `.planning/phases/09-developer-tooling/09-01-SUMMARY.md` with:
- Accomplishments: Introspection API created
- Issues: Any problems encountered
- Deviations: Changes from plan (if any)
- Next Phase Readiness: Is 09-02 ready to start (yes - independent)
</output>
