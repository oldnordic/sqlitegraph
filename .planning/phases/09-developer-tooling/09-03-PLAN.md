---
phase: 09-developer-tooling
plan: 03
type: execute
depends_on: ["01", "02"]
files_modified: [sqlitegraph-cli/src/cli.rs, sqlitegraph-cli/src/client.rs]
autonomous: true
---

<objective>
Add CLI commands for debugging, introspection, and profiling.

Purpose: Provide developer-friendly CLI tools for inspecting graph state and analyzing performance.
Output: New debug-* CLI commands using introspection and progress APIs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-developer-tooling/09-DISCOVERY.md
@.planning/phases/09-developer-tooling/09-01-SUMMARY.md
@.planning/phases/09-developer-tooling/09-02-SUMMARY.md
@/home/feanor/Projects/sqlitegraph/sqlitegraph-cli/src/cli.rs
@/home/feanor/Projects/sqlitegraph/sqlitegraph-cli/src/client.rs
@/home/feanor/Projects/sqlitegraph/sqlitegraph/src/introspection.rs
@/home/feanor/Projects/sqlitegraph/sqlitegraph/src/progress.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add debug-stats CLI command</name>
  <files>sqlitegraph-cli/src/cli.rs, sqlitegraph-cli/src/client.rs</files>
  <action>Add debug-stats command to CLI:

1. Update cli.rs CommandLineConfig::help():
   - Add "debug-stats" to commands list
   - Add description: "Show graph introspection data (JSON)"

2. Add handler in client.rs:
   ```rust
   "debug-stats" => {
       let intro = graph.introspect()?;
       let json = serde_json::to_string_pretty(&intro)?;
       println!("{}", json);
       Ok(())
   }
   ```

3. Optional: Add --format (json|table) flag
   - Default: JSON for LLM consumption
   - Table: Human-readable table format

Output includes:
- Backend type
- Node/edge counts
- Cache statistics (hits, misses, hit ratio)
- File sizes (if applicable)

DO NOT require additional CLI dependencies (use existing serde_json).
DO format JSON with proper indentation (to_string_pretty).</action>
  <verify>cargo run -- debug-stats works on in-memory database
cargo run -- debug-stats outputs valid JSON</verify>
  <done>debug-stats command returns structured introspection data</done>
</task>

<task type="auto">
  <name>Task 2: Add debug-dump CLI command</name>
  <files>sqlitegraph-cli/src/cli.rs, sqlitegraph-cli/src/client.rs</files>
  <action>Add debug-dump command for graph structure inspection:

1. Update cli.rs CommandLineConfig::help():
   - Add "debug-dump --output PATH" to commands
   - Description: "Export graph structure for debugging"

2. Add handler in client.rs:
   - Accepts --output flag (file path)
   - Dumps: all nodes with properties, all edges with metadata
   - Format: JSON lines (one JSON object per line) for large graphs
   - Or: Single JSON array for small graphs (< 1000 nodes)

3. Optional: Add --format (jsonl|json) flag
   - jsonl: One line per entity/edge (streaming, memory efficient)
   - json: Single array (small graphs only)

Structure:
```json
{"type": "node", "id": 123, "labels": ["Person"], "properties": {...}}
{"type": "edge", "id": 456, "from": 123, "to": 789, "edge_type": "KNOWS"}
```

DO NOT load entire graph into memory for --format jsonl (stream output).
DO validate --output path is writable.</action>
  <verify>cargo run -- debug-dump --output /tmp/graph.jsonl creates file
Output file is valid JSONL</verify>
  <done>debug-dump exports graph structure correctly</done>
</task>

<task type="auto">
  <name>Task 3: Add debug-trace and existing commands progress</name>
  <files>sqlitegraph-cli/src/cli.rs, sqlitegraph-cli/src/client.rs</files>
  <action>Add debug-trace command and progress bars to existing algorithms:

1. Add debug-trace command:
   - Enables trace logging for a specific operation
   - Usage: debug-trace bfs --start 123 --max-depth 3
   - Sets RUST_LOG=debug for the command duration
   - Shows detailed operation logs

2. Add ConsoleProgress to algorithm CLI commands:
   - bfs: Show "BFS: visited X nodes"
   - k-hop: Show "K-hop: processing level X"
   - shortest-path: Show "Searching: X nodes visited"
   - pagerank (new): "PageRank iteration X/Y"

3. Update algorithm commands to use *_with_progress variants:
   - Import ConsoleProgress from sqlitegraph::progress
   - Pass ConsoleProgress::new() to instrumented algorithms

For existing commands that don't have instrumented variants:
- Create simple progress reporting (e.g., "Processed X entities")

DO NOT enable trace logging by default (only via debug-trace).
DO keep progress output on stderr (stdout reserved for data).</action>
  <verify>cargo run -- debug-trace bfs --start 1 --max-depth 2 shows logs
cargo run -- bfs shows progress on stderr</verify>
  <done>debug-trace enables logging, algorithms show progress</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo run -- debug-stats shows introspection data
- [ ] cargo run -- debug-dump exports valid JSONL/JSON
- [ ] cargo run -- debug-trace bfs enables trace logging
- [ ] Algorithm commands show progress bars
- [ ] No regressions in existing CLI commands
</verification>

<success_criteria>

- debug-stats command outputs structured JSON introspection
- debug-dump exports graph structure for debugging
- debug-trace enables logging for specific operations
- Progress bars show for long-running operations
- All new CLI commands are documented in help text

</success_criteria>

<output>
After completion, create `.planning/phases/09-developer-tooling/09-03-SUMMARY.md` with:
- Accomplishments: CLI debug commands added
- Issues: Any problems encountered
- Deviations: Changes from plan (if any)
- Next Phase Readiness: Phase 9 complete, ready for Phase 10 (Testing & Docs)
</output>
