---
phase: 18-code-structure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sqlitegraph/src/hnsw/index.rs
  - sqlitegraph/src/hnsw/mod.rs
  - sqlitegraph/src/hnsw/index_api.rs
  - sqlitegraph/src/hnsw/index_persist.rs
  - sqlitegraph/src/hnsw/index_internal.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "hnsw/index.rs is split into focused API, persistence, and internal modules"
    - "All HnswIndex public methods remain accessible"
    - "HNSW tests pass after refactoring"
    - "Multi-layer functionality preserved"
  artifacts:
    - path: "sqlitegraph/src/hnsw/index.rs"
      provides: "Core HnswIndex struct and basic impl"
      max_lines: 600
      contains: "pub struct HnswIndex"
    - path: "sqlitegraph/src/hnsw/index_api.rs"
      provides: "Public API methods (insert, search, get)"
      min_lines: 300
      contains: "fn insert_vector|fn search"
    - path: "sqlitegraph/src/hnsw/index_persist.rs"
      provides: "Persistence operations (save/load)"
      min_lines: 200
      contains: "fn save|fn load"
    - path: "sqlitegraph/src/hnsw/index_internal.rs"
      provides: "Internal helpers (layer management, ID translation)"
      min_lines: 200
      contains: "fn get_local_id_for_layer|fn insert_into_layer"
  key_links:
    - from: "sqlitegraph/src/hnsw/index.rs"
      to: "index_api.rs, index_persist.rs, index_internal.rs"
      via: "impl blocks split across modules"
      pattern: "impl HnswIndex"
    - from: "sqlitegraph/src/hnsw/mod.rs"
      to: "index.rs and index_* modules"
      via: "mod declarations in mod.rs"
      pattern: "mod index_api;|mod index_persist;|mod index_internal;"
---

<objective>
Split `hnsw/index.rs` (2006 LOC) into focused modules: API methods, persistence operations, and internal helpers.

Purpose: Reduce index.rs from 2006 LOC to ~600 LOC by separating concerns (public API, persistence, internal layer operations). The index.rs file has grown significantly since the original 1605 LOC measurement due to multi-layer support additions.

Output: Core HnswIndex struct remains in index.rs (~600 LOC), with impl blocks split across index_api.rs (public methods), index_persist.rs (save/load), and index_internal.rs (layer helpers).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-code-structure/18-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@sqlitegraph/src/hnsw/mod.rs
@sqlitegraph/src/hnsw/index.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze index.rs structure and create module files</name>
  <files>
    sqlitegraph/src/hnsw/index_api.rs
    sqlitegraph/src/hnsw/index_persist.rs
    sqlitegraph/src/hnsw/index_internal.rs
  </files>
  <action>
    First, read the current index.rs to understand the impl block structure:

    1. Identify all impl blocks in index.rs and categorize by purpose:
       - Core struct definition and field declarations
       - Public API methods (new, insert_vector, search, get)
       - Persistence methods (save_to_file, load_from_file, save_index, load_index)
       - Internal layer helpers (get_local_id_for_layer, insert_into_layer, layer management)

    2. Create `hnsw/index_api.rs` containing:
       - Public API impl block with:
         - `pub fn new(...)`
         - `pub fn insert_vector(...)`
         - `pub fn search(...)`
         - `pub fn search_with_candidates(...)`
         - `pub fn get(...)`
         - `pub fn len(...)`
         - `pub fn is_empty(...)`
         - Any other user-facing public methods
       - Include module documentation

    3. Create `hnsw/index_persist.rs` containing:
       - Persistence impl block with:
         - `pub fn save_to_file(...)`
         - `pub fn load_from_file(...)`
         - `fn save_index(...)`
         - `fn load_index(...)`
         - Any other save/load related methods
       - Include module documentation

    4. Create `hnsw/index_internal.rs` containing:
       - Internal helper methods:
         - `fn get_local_id_for_layer(...)`
         - `fn get_global_id_for_layer(...)`
         - `fn insert_into_layer(...)`
         - Layer management and ID translation helpers
       - Multi-layer specific helpers
       - Include module documentation

    Use the pattern from existing hnsw modules (layer.rs, neighborhood.rs) as reference.
  </action>
  <verify>
    cargo check --package sqlitegraph --lib 2>&1 | head -30
  </verify>
  <done>
    Three new module files created with impl blocks categorized by purpose.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update hnsw/mod.rs to include new modules</name>
  <files>
    sqlitegraph/src/hnsw/mod.rs
    sqlitegraph/src/hnsw/index.rs
  </files>
  <action>
    1. Update `hnsw/mod.rs` to add module declarations:
       ```rust
       // Existing modules
       mod builder;
       mod config;
       mod distance_metric;
       mod errors;
       mod index;  // Core struct
       mod index_api;  // New
       mod index_persist;  // New
       mod index_internal;  // New
       mod layer;
       mod multilayer;
       mod neighborhood;
       mod storage;
       ```

    2. Reduce index.rs to contain only:
       - Module documentation
       - `pub struct HnswIndex` with field declarations
       - Constructor if not in index_api.rs
       - Keep core impl block minimal (~600 LOC target)

    3. Move impl blocks to the new module files while maintaining:
       - All `impl HnswIndex` syntax
       - All `use` statements at top of each file
       - Proper access modifiers (pub for public API, private for internal)

    4. Verify no duplicate methods between files
  </action>
  <verify>
    cargo check --package sqlitegraph --lib 2>&1 | head -30
  </verify>
  <done>
    hnsw/mod.rs updated with new module declarations. index.rs reduced to ~600 LOC with only core struct and minimal impl.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify HNSW functionality after split</name>
  <files>
    sqlitegraph/src/hnsw/index.rs
    sqlitegraph/src/hnsw/index_api.rs
    sqlitegraph/src/hnsw/index_persist.rs
    sqlitegraph/src/hnsw/index_internal.rs
  </files>
  <action>
    Run full HNSW test suite to ensure functionality preserved:

    1. Run HNSW tests: `cargo test --package sqlitegraph --lib hnsw`
    2. Verify multi-layer functionality (critical since this was recent work)
    3. Verify persistence (save/load) still works
    4. Run HNSW benchmarks to confirm no performance regression: `cargo test --package sqlitegraph --test '*' -- --ignored`

    Check specifically for:
    - Multi-layer insertion working
    - Greedy descent search working
    - Layer ID translation working
    - Persistence save/load working
  </action>
  <verify>
    cargo test --package sqlitegraph --lib hnsw 2>&1 | tail -30
  </verify>
  <done>
    All HNSW tests pass. Multi-layer functionality verified. No performance regression. index.rs ~600 LOC, index_api.rs ~400 LOC, index_persist.rs ~300 LOC, index_internal.rs ~400 LOC.
  </done>
</task>

</tasks>

<verification>
1. index.rs reduced to ~600 LOC (from 2006 LOC)
2. All public API methods remain accessible on HnswIndex
3. HNSW tests pass without modification
4. Multi-layer functionality preserved
5. Persistence (save/load) working
6. Module structure follows hnsw/ conventions
</verification>

<success_criteria>
- index.rs split into 4 files (index.rs, index_api.rs, index_persist.rs, index_internal.rs)
- Each file under 600 LOC
- All HNSW tests pass
- Multi-layer and persistence functionality preserved
- No API changes for consumers
</success_criteria>

<output>
After completion, create `.planning/phases/18-code-structure/18-02-SUMMARY.md` with:
- Module structure created
- Files created with LOC counts
- Test results
- Multi-layer verification results
</output>
