---
phase: 18-code-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sqlitegraph/src/algo.rs
  - sqlitegraph/src/algo/mod.rs
  - sqlitegraph/src/algo/centrality.rs
  - sqlitegraph/src/algo/community.rs
  - sqlitegraph/src/algo/structure.rs
  - sqlitegraph/src/algo/tests.rs
  - sqlitegraph/src/lib.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "algo.rs is split into focused modules by algorithm category"
    - "All algorithm functions remain accessible via public API"
    - "All tests pass after refactoring"
    - "No changes to algorithm behavior or outputs"
  artifacts:
    - path: "sqlitegraph/src/algo/mod.rs"
      provides: "Module re-exports and documentation"
      contains: "pub use centrality::*, pub use community::*, pub use structure::*"
    - path: "sqlitegraph/src/algo/centrality.rs"
      provides: "PageRank and Betweenness centrality algorithms"
      min_lines: 200
    - path: "sqlitegraph/src/algo/community.rs"
      provides: "Label propagation and Louvain community detection"
      min_lines: 400
    - path: "sqlitegraph/src/algo/structure.rs"
      provides: "Connected components, cycles, degree ranking"
      min_lines: 150
    - path: "sqlitegraph/src/algo/tests.rs"
      provides: "All algorithm tests"
      contains: "#[cfg(test)]"
  key_links:
    - from: "sqlitegraph/src/algo/mod.rs"
      to: "algo/centrality.rs, algo/community.rs, algo/structure.rs"
      via: "mod declarations and pub use re-exports"
      pattern: "mod (centrality|community|structure);"
    - from: "sqlitegraph/src/lib.rs"
      to: "algo/mod.rs"
      via: "pub mod algo declaration"
      pattern: "pub mod algo;"
---

<objective>
Split `algo.rs` (1398 LOC) into focused modules by algorithm category (centrality, community, structure).

Purpose: Reduce file size to improve maintainability and follow project conventions (< 600 LOC per file). The algo.rs file is the simplest splitting case as it has no complex V2 backend integration, making it ideal as a "warm-up" refactoring.

Output: A new `algo/` module directory with `mod.rs`, `centrality.rs`, `community.rs`, `structure.rs`, and `tests.rs` files. All public API functions remain accessible at `crate::algo::*`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-code-structure/18-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@sqlitegraph/src/algo.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create algo module directory structure</name>
  <files>
    sqlitegraph/src/algo/mod.rs
    sqlitegraph/src/algo/centrality.rs
    sqlitegraph/src/algo/community.rs
    sqlitegraph/src/algo/structure.rs
    sqlitegraph/src/algo/tests.rs
  </files>
  <action>
    Create the new module directory structure:

    1. Create `sqlitegraph/src/algo/` directory

    2. Create `algo/mod.rs` with:
       - Module documentation (move from algo.rs header)
       - `pub use` re-exports for all public functions
       - `mod` declarations for submodules
       - `#[cfg(test)] mod tests;` declaration

    3. Create `algo/centrality.rs` containing:
       - `pub fn pagerank(...)`
       - `pub fn pagerank_with_progress<F>(...)`
       - `pub fn betweenness_centrality(...)`
       - `pub fn betweenness_centrality_with_progress<F>(...)`
       - Move all helper functions used only by centrality algorithms
       - Include module documentation

    4. Create `algo/community.rs` containing:
       - `pub fn label_propagation(...)`
       - `pub fn louvain_communities(...)`
       - `pub fn louvain_communities_with_progress<F>(...)`
       - Move all helper functions used only by community algorithms
       - Include module documentation

    5. Create `algo/structure.rs` containing:
       - `pub fn connected_components(...)`
       - `pub fn find_cycles_limited(...)`
       - `pub fn nodes_by_degree(...)`
       - Move `fn normalize_cycles(...)` helper
       - Include module documentation

    6. Create `algo/tests.rs` containing:
       - Move all `#[cfg(test)]` tests from algo.rs
       - Ensure each test module can access the algorithms via `super::`

    Follow the existing hnsw/mod.rs pattern for module organization.

    Do NOT modify algorithm logic - only move code.
  </action>
  <verify>
    cargo test --package sqlitegraph --lib algo
  </verify>
  <done>
    All algo module files created with proper mod.rs re-exports. Module structure matches hnsw/ pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update lib.rs and delete old algo.rs</name>
  <files>
    sqlitegraph/src/lib.rs
    sqlitegraph/src/algo.rs
  </files>
  <action>
    1. Update `sqlitegraph/src/lib.rs`:
       - Change `pub mod algo;` to point to the new module directory
       - The declaration remains the same, just ensure it works with the new directory structure

    2. Delete the old `sqlitegraph/src/algo.rs` file after verifying the new module works

    3. Run `cargo check --package sqlitegraph --lib` to verify compilation
  </action>
  <verify>
    cargo check --package sqlitegraph --lib && cargo test --package sqlitegraph --lib algo
  </verify>
  <done>
    Old algo.rs deleted, lib.rs references new module directory, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify algorithm behavior unchanged</name>
  <files>
    sqlitegraph/src/algo/mod.rs
    sqlitegraph/src/algo/centrality.rs
    sqlitegraph/src/algo/community.rs
    sqlitegraph/src/algo/structure.rs
  </files>
  <action>
    Run full algorithm test suite to ensure no behavior changes:

    1. Run all algo tests: `cargo test --package sqlitegraph --lib algo`
    2. Run integration tests that use algorithms
    3. Verify public API is unchanged: all functions accessible at `crate::algo::*`

    Confirm expected outputs are identical by checking tests pass without modification.
  </action>
  <verify>
    cargo test --package sqlitegraph --lib algo 2>&1 | tail -20
  </verify>
  <done>
    All algo tests pass. Public API unchanged (pagerank, betweenness_centrality, label_propagation, louvain_communities, connected_components, find_cycles_limited, nodes_by_degree all accessible at crate::algo::*).
  </done>
</task>

</tasks>

<verification>
1. Module structure follows project conventions (hnsw/ as reference)
2. All public algorithm functions remain accessible at crate::algo::*
3. All algo tests pass without modification
4. No changes to algorithm behavior or outputs
5. File sizes: centrality.rs ~400 LOC, community.rs ~500 LOC, structure.rs ~200 LOC, tests.rs ~200 LOC
</verification>

<success_criteria>
- algo.rs split into 4 files (mod.rs, centrality.rs, community.rs, structure.rs, tests.rs)
- Each file under 600 LOC
- All public API functions accessible at crate::algo::*
- All tests pass
- No algorithm behavior changes
</success_criteria>

<output>
After completion, create `.planning/phases/18-code-structure/18-01-SUMMARY.md` with:
- Module structure created
- Files created with LOC counts
- Test results
- Any issues encountered
</output>
