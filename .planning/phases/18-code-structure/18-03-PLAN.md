---
phase: 18-code-structure
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/mod.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/node_ops.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/edge_ops.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/cluster_ops.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/string_ops.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/header_ops.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/free_space_ops.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator/mod.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator/node_validation.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator/edge_validation.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator/cluster_validation.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator/string_validation.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator/free_space_validation.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/validator/cross_record.rs
  - sqlitegraph/src/backend/native/v2/wal/recovery/mod.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "rollback.rs is split into operation-specific modules"
    - "validator.rs is split into validation-specific modules"
    - "All rollback and validation functions work correctly"
    - "WAL recovery tests pass"
  artifacts:
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/mod.rs"
      provides: "RollbackSystem struct and coordination"
      contains: "pub struct RollbackSystem"
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/node_ops.rs"
      provides: "Node rollback operations"
      contains: "rollback_node_insert|rollback_node_update|rollback_node_delete"
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/edge_ops.rs"
      provides: "Edge rollback operations"
      contains: "rollback_edge_insert|rollback_edge_update|rollback_edge_delete"
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/validator/mod.rs"
      provides: "TransactionValidator struct and coordination"
      contains: "pub struct TransactionValidator"
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/validator/node_validation.rs"
      provides: "Node record validation"
      contains: "validate_node_insert|validate_node_update|validate_node_delete"
    - path: "sqlitegraph/src/backend/native/v2/wal/recovery/validator/cross_record.rs"
      provides: "Cross-record consistency checks"
      contains: "validate_cross_record_consistency"
  key_links:
    - from: "sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/mod.rs"
      to: "node_ops.rs, edge_ops.rs, cluster_ops.rs, etc."
      via: "mod declarations and impl RollbackSystem delegation"
      pattern: "mod (node|edge|cluster)_ops;"
    - from: "sqlitegraph/src/backend/native/v2/wal/recovery/replayer/mod.rs"
      to: "rollback/mod.rs"
      via: "mod rollback declaration"
      pattern: "pub mod rollback;"
    - from: "sqlitegraph/src/backend/native/v2/wal/recovery/mod.rs"
      to: "validator/mod.rs"
      via: "pub mod validator declaration"
      pattern: "pub mod validator;"
---

<objective>
Split `rollback.rs` (1912 LOC) and `validator.rs` (1509 LOC) into focused modules by operation type.

Purpose: Reduce WAL recovery module file sizes for maintainability. Rollback operations and validations are organized by the V2 component type they handle (node, edge, cluster, string, header, free_space). This matches the existing operations/ subdirectory pattern in the replayer module.

Output: New rollback/ and validator/ subdirectories with operation-specific modules. Each file handles one category of rollback/validation operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-code-structure/18-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@sqlitegraph/src/backend/native/v2/wal/recovery/replayer/mod.rs
@sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback.rs
@sqlitegraph/src/backend/native/v2/wal/recovery/validator.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rollback subdirectory module structure</name>
  <files>
    sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/mod.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/node_ops.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/edge_ops.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/cluster_ops.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/string_ops.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/header_ops.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/replayer/rollback/free_space_ops.rs
  </files>
  <action>
    Split rollback.rs (1912 LOC) into operation-specific modules:

    1. Create `rollback/` directory under `replayer/`

    2. Move current rollback.rs content to `rollback/mod.rs` initially, then refactor

    3. Create `rollback/node_ops.rs` containing:
       - `fn rollback_node_insert(...)`
       - `fn rollback_node_update(...)`
       - `fn rollback_node_delete(...)`
       - Any helpers specific to node rollback
       - Include module documentation

    4. Create `rollback/edge_ops.rs` containing:
       - `fn rollback_edge_insert(...)`
       - `fn rollback_edge_update(...)`
       - `fn rollback_edge_delete(...)`
       - Any helpers specific to edge rollback

    5. Create `rollback/cluster_ops.rs` containing:
       - `fn rollback_cluster_create(...)`
       - Any cluster-specific rollback helpers

    6. Create `rollback/string_ops.rs` containing:
       - `fn rollback_string_insert(...)`

    7. Create `rollback/header_ops.rs` containing:
       - `fn rollback_header_update(...)`

    8. Create `rollback/free_space_ops.rs` containing:
       - `fn rollback_free_space_allocate(...)`
       - `fn rollback_free_space_deallocate(...)`

    9. Update `rollback/mod.rs` to contain:
       - `pub struct RollbackSystem` definition
       - Core RollbackSystem methods (constructor, execute_rollback)
       - Module declarations for each ops file
       - RollbackSummary struct if present

    Follow the existing `replayer/operations/` pattern for organization.
  </action>
  <verify>
    cargo check --package sqlitegraph --lib 2>&1 | grep -E "error|warning" | head -20
  </verify>
  <done>
    Rollback directory created with 7 module files. RollbackSystem struct in mod.rs with operation methods delegated to ops modules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update replayer/mod.rs to reference rollback module</name>
  <files>
    sqlitegraph/src/backend/native/v2/wal/recovery/replayer/mod.rs
  </files>
  <action>
    Update `replayer/mod.rs` to use the new rollback module structure:

    1. Change from single-file rollback to module directory:
       - If currently: `mod rollback;` (referencing rollback.rs)
       - Update to: `pub mod rollback;` (referencing rollback/ directory)

    2. Verify replayer exports still work:
       - RollbackSystem should be accessible at `crate::backend::native::v2::wal::recovery::replayer::rollback::RollbackSystem`
       - Or re-exported at appropriate level

    3. Update any `use` statements in other files that reference rollback

    4. Run cargo check to verify compilation
  </action>
  <verify>
    cargo check --package sqlitegraph --lib 2>&1 | head -20
  </verify>
  <done>
    replayer/mod.rs updated. Rollback module accessible. All references updated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create validator subdirectory module structure</name>
  <files>
    sqlitegraph/src/backend/native/v2/wal/recovery/validator/mod.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/validator/node_validation.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/validator/edge_validation.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/validator/cluster_validation.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/validator/string_validation.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/validator/free_space_validation.rs
    sqlitegraph/src/backend/native/v2/wal/recovery/validator/cross_record.rs
  </files>
  <action>
    Split validator.rs (1509 LOC) into validation-specific modules:

    1. Create `validator/` directory under `recovery/`

    2. Move current validator.rs to `validator/mod.rs` initially, then refactor

    3. Create `validator/node_validation.rs` containing:
       - `fn validate_node_insert(...)`
       - `fn validate_node_update(...)`
       - `fn validate_node_delete(...)`
       - Node-specific validation helpers

    4. Create `validator/edge_validation.rs` containing:
       - `fn validate_edge_insert(...)`
       - `fn validate_edge_update(...)`
       - `fn validate_edge_delete(...)`
       - Edge-specific validation helpers

    5. Create `validator/cluster_validation.rs` containing:
       - `fn validate_cluster_create(...)`

    6. Create `validator/string_validation.rs` containing:
       - `fn validate_string_insert(...)`

    7. Create `validator/free_space_validation.rs` containing:
       - `fn validate_free_space_allocate(...)`
       - `fn validate_free_space_deallocate(...)`

    8. Create `validator/cross_record.rs` containing:
       - `fn validate_cross_record_consistency(...)`
       - `fn validate_v2_invariants(...)`

    9. Update `validator/mod.rs` to contain:
       - `pub enum ValidationResult`
       - `pub enum ValidationSeverity`
       - `pub struct ValidationStatistics`
       - `pub struct TransactionValidator` definition
       - Core validation entry points
       - `pub struct RecoveryValidator` if present
       - Module declarations

    Follow the same pattern as rollback/ for consistency.
  </action>
  <verify>
    cargo check --package sqlitegraph --lib 2>&1 | head -20
  </verify>
  <done>
    Validator directory created with 7 module files. TransactionValidator struct in mod.rs with validation methods delegated to validation modules.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update recovery/mod.rs and verify WAL recovery</name>
  <files>
    sqlitegraph/src/backend/native/v2/wal/recovery/mod.rs
  </files>
  <action>
    1. Update `recovery/mod.rs` to use new validator module:
       - Change to: `pub mod validator;` (referencing validator/ directory)

    2. Run full WAL recovery test suite:
       - `cargo test --package sqlitegraph --lib wal`
       - `cargo test --package sqlitegraph --lib recovery`
       - `cargo test --package sqlitegraph --lib rollback`

    3. Verify specific rollback scenarios:
       - Node deletion rollback (critical ACID functionality)
       - Edge operations rollback
       - Cluster operations rollback

    4. Verify validation still works:
       - Pre-commit validation
       - Post-recovery validation
  </action>
  <verify>
    cargo test --package sqlitegraph --lib recovery 2>&1 | tail -30
  </verify>
  <done>
    recovery/mod.rs updated. All WAL recovery tests pass. Rollback and validation functionality preserved.
  </done>
</task>

</tasks>

<verification>
1. rollback.rs split into 7 files (mod.rs + 6 ops modules)
2. validator.rs split into 7 files (mod.rs + 6 validation modules)
3. Each file under 400 LOC
4. WAL recovery tests pass
5. Rollback operations work correctly (especially node deletion)
6. Validation still functional
</verification>

<success_criteria>
- rollback/ module created with operation-specific files
- validator/ module created with validation-specific files
- All files under 400 LOC
- WAL recovery tests pass
- No behavior changes to rollback or validation
</success_criteria>

<output>
After completion, create `.planning/phases/18-code-structure/18-03-SUMMARY.md` with:
- Module structure created
- Files created with LOC counts
- Test results
- Rollback verification results
</output>
