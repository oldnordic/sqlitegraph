---
phase: 08-graph-algorithms
plan: 03
type: execute
depends_on: ["01", "02"]
files_modified: [sqlitegraph/benches/algo_benchmarks.rs]
autonomous: true
---

<objective>
Create comprehensive benchmarks and edge case tests for all graph algorithms.

Purpose: Validate algorithm performance, establish regression detection, and ensure edge case handling.
Output: New benchmark suite with Criterion framework, extended test coverage.

Benchmarks measure performance across different graph sizes and topologies (random, scale-free, small-world).
Tests cover edge cases (empty graphs, disconnected components, convergence scenarios).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-graph-algorithms/08-DISCOVERY.md
@.planning/phases/08-graph-algorithms/08-01-SUMMARY.md
@.planning/phases/08-graph-algorithms/08-02-SUMMARY.md
@/home/feanor/Projects/sqlitegraph/sqlitegraph/src/algo.rs
@/home/feanor/Projects/sqlitegraph/sqlitegraph/tests/algo_tests.rs
@/home/feanor/Projects/sqlitegraph/sqlitegraph/benches/read_path_benchmarks.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create algorithm benchmark suite</name>
  <files>sqlitegraph/benches/algo_benchmarks.rs</files>
  <action>Create benches/algo_benchmarks.rs with Criterion benchmarks:
  - Follow pattern from existing benches/read_path_benchmarks.rs
  - Use criterion::{criterion_group, criterion_main, BenchmarkId, Criterion}
  - Benchmark groups:
    1. pagerank_benchmarks: sizes [10, 100, 1000] nodes, random graphs
    2. betweenness_benchmarks: sizes [10, 100, 500] nodes (O(VE) expensive - smaller max)
    3. label_prop_benchmarks: sizes [10, 100, 1000, 5000] nodes
    4. louvain_benchmarks: sizes [10, 100, 1000] nodes
  - Graph generators:
    - random_graph(n, edge_probability): creates random edges
    - star_graph(n): center node connected to all others
    - cycle_graph(n): ring topology
    - barbell_graph(n): two cliques connected by single edge
  - For each benchmark: measure time and compare across sizes
  - Include sample_size parameter for betweenness (test exact vs sampled)

  DO NOT benchmark with >5000 nodes for betweenness (too slow).
  DO include baseline comparisons for detecting performance regressions.</action>
  <verify>cargo bench --bench algo_benchmarks runs successfully
Criterion HTML report generates with all benchmark groups
Benchmark runs complete in reasonable time (< 5 min total)</verify>
  <done>algo_benchmarks.rs file created and compiles
All 4 benchmark groups run successfully
Criterion output shows timing across different graph sizes</done>
</task>

<task type="auto">
  <name>Task 2: Add edge case and stress tests</name>
  <files>sqlitegraph/tests/algo_tests.rs</files>
  <action>Add comprehensive edge case tests to algo_tests.rs:
  - Empty graph tests:
    * test_pagerank_empty_graph: returns empty result
    * test_betweenness_empty_graph: returns empty result
    * test_label_prop_empty_graph: returns empty result
    * test_louvain_empty_graph: returns empty result
  - Single node tests:
    * test_pagerank_single_node: returns score 1.0
    * test_betweenness_single_node: returns 0.0 (no paths)
  - Disconnected components tests:
    * test_pagerank_disconnected: handles multiple components
    * test_betweenness_disconnected: nodes in different components have 0 betweenness
  - Convergence tests:
    * test_label_prop_max_iterations: stops when converged (before max)
    * test_louvain_max_iterations: stops when stable (before max)
  - Large graph stress test (1000 nodes):
    * test_pagerank_large_graph: completes in reasonable time
    * test_label_prop_large_graph: completes in reasonable time

  Follow existing test patterns with insert_entity/insert_edge helpers.
  Use #[cfg_attr(not(feature = "benchmark"), ignore)] for large/slow tests if needed.</action>
  <verify>cargo test algo_tests passes (all new tests + existing 15 tests)
Large graph tests complete in < 10 seconds each
No memory leaks or panics</verify>
  <done>All edge case tests pass
Large graph tests complete successfully
Total algo_tests count: 15 existing + ~10 new = ~25 tests</done>
</task>

<task type="auto">
  <name>Task 3: Add algorithm documentation and examples</name>
  <files>sqlitegraph/src/algo.rs</files>
  <action>Add rustdoc documentation to all algorithm functions:
  - Function-level docs explaining algorithm, parameters, return values
  - Complexity notes (time/space complexity)
  - Usage examples in doc comments
  - Links to Wikipedia or algorithm references
  - Caveats about performance (e.g., betweenness is O(VE))

  Example format:
  /// Computes PageRank scores for all nodes in the graph.
  ///
  /// PageRank measures node importance based on the link structure.
  /// Nodes with many incoming links from important nodes receive higher scores.
  ///
  /// # Arguments
  /// * `damping` - Probability of continuing random walk (0.85 is typical)
  /// * `iterations` - Number of power iterations (20-50 recommended)
  ///
  /// # Returns
  /// Vector of (node_id, score) pairs sorted by score descending.
  ///
  /// # Complexity
  /// Time: O(k * |E|) where k is iterations
  /// Space: O(|V|)
  ///
  /// # Example
  /// ```rust
  /// use sqlitegraph::{SqliteGraph, algo::pagerank};
  /// let graph = SqliteGraph::open_in_memory()?;
  /// // ... add nodes and edges ...
  /// let scores = pagerank(&graph, 0.85, 20)?;
  /// ```
  pub fn pagerank(...) ...

  Add similar docs for betweenness_centrality, louvain_communities, label_propagation.</action>
  <verify>cargo doc generates documentation without warnings
rustdoc --no-deps --document-private-items shows algorithm docs
Example code in docs compiles and is clear</verify>
  <done>All 4 algorithm functions have comprehensive rustdoc
cargo doc produces no warnings
Documentation examples are clear and accurate</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo bench --bench algo_benchmarks runs successfully
- [ ] All new tests pass (edge cases + stress tests)
- [ ] cargo doc completes without warnings
- [ ] Benchmark suite includes all 4 algorithms
- [ ] Large graph tests (1000 nodes) complete in reasonable time
</verification>

<success_criteria>
- algo_benchmarks.rs created with 4 benchmark groups
- ~10 new edge case tests added to algo_tests.rs
- All algorithm functions have comprehensive rustdoc
- Benchmarks establish performance baseline for regression detection
</success_criteria>

<output>
After completion, create `.planning/phases/08-graph-algorithms/08-03-SUMMARY.md` with:
- Accomplishments: Benchmarks created, tests added, documentation complete
- Performance Baselines: Record benchmark timings for each algorithm
- Next Phase Readiness: Phase 8 complete, ready for Phase 9 (Developer Tooling)
</output>
