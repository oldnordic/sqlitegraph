---
phase: 19-concurrent-features
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - sqlitegraph/benches/connection_pool.rs
  - sqlitegraph/Cargo.toml
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Benchmark measures connection checkout latency"
    - "Benchmark compares pooled vs direct connection overhead"
    - "Pool reuse shows benefit vs repeated open/close"
    - "Benchmark tests concurrent access patterns"
    - "Results demonstrate performance improvement from pooling"
  artifacts:
    - path: "sqlitegraph/benches/connection_pool.rs"
      provides: "Criterion benchmark for connection pool performance"
      min_lines: 80
      contains: "criterion_group", "bench_checkout", "bench_concurrent"
    - path: "sqlitegraph/Cargo.toml"
      provides: "Benchmark registration"
      contains: "[[bench]]", "connection_pool"
  key_links:
    - from: "sqlitegraph/benches/connection_pool.rs"
      to: "sqlitegraph/src/graph/pool.rs"
      via: "use sqlitegraph::graph::pool::PoolManager"
      pattern: "PoolManager"
    - from: "sqlitegraph/benches/connection_pool.rs"
      to: "sqlitegraph/src/graph/core.rs"
      via: "SqliteGraph::open_with_config()"
      pattern: "open_with_config"
---

<objective>
Add benchmarks measuring connection pool performance benefits, demonstrating reduced overhead from connection reuse.

Purpose: Verify that connection pooling actually improves performance. Benchmarks quantify the benefit of reuse vs repeated open/close.

Output: Criterion benchmark suite measuring checkout latency, concurrent access, and comparison with direct connections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-concurrent-features/19-RESEARCH.md
@.planning/phases/19-concurrent-features/19-01-PLAN.md
@.planning/phases/19-concurrent-features/19-02-PLAN.md

# Existing benchmarks (sqlitegraph/Cargo.toml:60-89)
[[bench]]
name = "bfs"
harness = false

[[bench]]
name = "k_hop"
harness = false

[[bench]]
name = "insert"
harness = false

[[bench]]
name = "hnsw"
harness = false

[[bench]]
name = "comprehensive_performance"
harness = false

[[bench]]
name = "wal_recovery_benchmarks"
harness = false

[[bench]]
name = "hnsw_multilayer"
harness = false

# Benchmark pattern (from existing benchmarks)
use criterion::{black_box, criterion_group, criterion_main, Criterion, BenchmarkId};

fn bench_something(c: &mut Criterion) {
    let mut group = c.benchmark_group("group_name");
    // benchmarks...
    group.finish();
}

criterion_group!(benches, bench_something);
criterion_main!(benches);
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create connection pool benchmark file</name>
  <files>sqlitegraph/benches/connection_pool.rs</files>
  <action>
    Create sqlitegraph/benches/connection_pool.rs with:

    1. **bench_checkout_latency** - Measure single connection checkout:
       - Baseline: Direct Connection::open() overhead
       - Pooled: pool.get() latency (first vs subsequent)
       - Expected: First checkout ~same as open(), subsequent ~5-10x faster

    2. **bench_concurrent_access** - Measure multi-threaded checkout:
       - 2, 4, 8 threads checking out connections
       - Measure contention vs throughput
       - Use rayon for parallel iteration

    3. **bench_query_throughput** - Compare pooled vs direct:
       - 1000 simple queries with pooled connections
       - 1000 simple queries with direct connection
       - Measure total time (pool should be faster)

    4. **bench_pool_sizes** - Compare different pool sizes:
       - Pool sizes: 1, 5, 10, 20 connections
       - Measure throughput with 8 concurrent threads
       - Find optimal size for concurrent workload

    Use Criterion's benchmark_group with parameterized inputs for pool sizes.
    Use tempfile for test databases to avoid interference.

    Reference: Other benchmarks in sqlitegraph/benches/ for Criterion patterns.
  </action>
  <verify>
    cargo check --benches passes (benchmark compiles)
  </verify>
  <done>
    connection_pool.rs exists with 4 benchmark groups measuring pool performance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register benchmark in Cargo.toml</name>
  <files>sqlitegraph/Cargo.toml</files>
  <action>
    Add benchmark registration after line 89:

    [[bench]]
    name = "connection_pool"
    harness = false

    Follows existing pattern for other benchmarks (bfs, k_hop, insert, hnsw, etc.).
  </action>
  <verify>
    cargo check --benches passes, cargo test --bench connection_pool runs benchmarks
  </verify>
  <done>
    connection_pool benchmark registered in Cargo.toml. Runs with cargo bench --bench connection_pool.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run benchmarks and verify results</name>
  <files>sqlitegraph/benches/connection_pool.rs</files>
  <action>
    1. Run benchmarks: cargo bench --bench connection_pool

    2. Verify results show expected patterns:
       - Subsequent checkouts faster than first (warm pool)
       - Pooled throughput >= direct connection (no regression)
       - Larger pools improve concurrent throughput (up to saturation)

    3. If results don't show expected benefit, investigate:
       - Pool may not be reusing connections (check config)
       - Benchmark may be measuring wrong thing (check query pattern)
       - SQLite contention may be bottleneck (add WAL mode check)

    4. Document findings in benchmark comments

    Criterion generates HTML reports in target/criterion/connection_pool/report/.
  </action>
  <verify>
    cargo bench --bench connection_pool completes successfully
    Results show connection reuse benefit (subsequent checkout < first checkout)
  </verify>
  <done>
    Benchmarks run successfully, demonstrating pool performance benefits.
  </done>
</task>

</tasks>

<verification>
1. connection_pool.rs benchmark file exists with Criterion setup
2. Benchmark registered in Cargo.toml
3. Benchmarks measure: checkout latency, concurrent access, query throughput, pool sizes
4. cargo bench --bench connection_pool runs successfully
5. Results demonstrate connection reuse is beneficial
</verification>

<success_criteria>
1. checkout_latency benchmark shows subsequent checkouts faster than first
2. concurrent_access benchmark measures multi-threaded checkout behavior
3. query_throughput compares pooled vs direct connection performance
4. pool_sizes benchmark shows impact of pool size on throughput
5. All benchmarks complete without errors and produce Criterion HTML reports
</success_criteria>

<output>
After completion, create `.planning/phases/19-concurrent-features/19-03-SUMMARY.md`
</output>
