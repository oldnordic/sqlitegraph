---
phase: 19-concurrent-features
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - sqlitegraph/src/config/sqlite.rs
  - sqlitegraph/src/graph/core.rs
  - sqlitegraph/src/graph/pool.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Pool size can be configured via SqliteConfig"
    - "Builder method with_pool_size() sets max connections"
    - "Default pool size of 5 is used when not configured"
    - "Pool size is passed from SqliteConfig to PoolManager"
    - "Configuration pattern matches existing SqliteConfig builder style"
  artifacts:
    - path: "sqlitegraph/src/config/sqlite.rs"
      provides: "pool_size field and with_pool_size() builder"
      contains: "pool_size: Option<usize>", "with_pool_size"
    - path: "sqlitegraph/src/graph/core.rs"
      provides: "open() accepts SqliteConfig for pool size"
      contains: "open(path, cfg)", "cfg.pool_size"
    - path: "sqlitegraph/src/graph/pool.rs"
      provides: "PoolManager accepts pool_size parameter"
      contains: "with_max_size", "max_size()"
  key_links:
    - from: "sqlitegraph/src/graph/core.rs::open()"
      to: "sqlitegraph/src/config/sqlite.rs::SqliteConfig"
      via: "cfg.pool_size.unwrap_or(5)"
      pattern: "pool_size"
    - from: "sqlitegraph/src/graph/core.rs::open()"
      to: "sqlitegraph/src/graph/pool.rs::PoolManager"
      via: "PoolManager::with_max_size()"
      pattern: "with_max_size"
---

<objective>
Add configurable pool size to SqliteConfig with builder pattern, allowing users to customize connection pool size for their workload.

Purpose: Default pool size (5) may not be optimal for all workloads. High-concurrency scenarios need larger pools; resource-constrained environments need smaller pools.

Output: `SqliteConfig::with_pool_size()` builder method, pool passed to `PoolManager` during initialization.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-concurrent-features/19-RESEARCH.md
@.planning/phases/19-concurrent-features/19-01-PLAN.md

# Existing SqliteConfig (sqlitegraph/src/config/sqlite.rs)
pub struct SqliteConfig {
    pub without_migrations: bool,
    pub cache_size: Option<usize>,
    pub pragma_settings: HashMap<String, String>,
}

impl SqliteConfig {
    pub fn with_cache_size(mut self, cache_size: usize) -> Self {
        self.cache_size = Some(cache_size);
        self
    }

    pub fn with_pragma(mut self, key: &str, value: &str) -> Self { ... }
}

# Pattern from RESEARCH.md (lines 162-184)
pub pool_size: Option<usize>,

impl SqliteConfig {
    pub fn with_pool_size(mut self, size: usize) -> Self {
        self.pool_size = Some(size);
        self
    }

    pub fn with_max_connections(mut self, max: usize) -> Self {
        self.pool_size = Some(max);
        self
}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pool_size field to SqliteConfig</name>
  <files>sqlitegraph/src/config/sqlite.rs</files>
  <action>
    1. Add `pub pool_size: Option<usize>` field to SqliteConfig struct

    2. Add builder method `with_pool_size(mut self, size: usize) -> Self`:
       - Sets self.pool_size = Some(size)
       - Returns self

    3. Add alias builder `with_max_connections(mut self, max: usize) -> Self`:
       - Delegates to with_pool_size() for API convenience
       - Some users prefer "max_connections" terminology

    Follow existing pattern from with_cache_size() and with_pragma() methods.
  </action>
  <verify>
    cargo check --lib passes (SqliteConfig compiles with new field)
  </verify>
  <done>
    pool_size field exists with with_pool_size() and with_max_connections() builders.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire pool_size through SqliteGraph::open()</name>
  <files>sqlitegraph/src/graph/core.rs</files>
  <action>
    1. Add new `open_with_config<P: AsRef<Path>>(path: P, cfg: &SqliteConfig)` method:
       - Reads cfg.pool_size.unwrap_or(5) for pool size
       - Passes size to PoolManager::with_max_size()
       - Uses cfg.without_migrations flag

    2. Update existing `open()` to delegate to open_with_config():
       - Calls open_with_config(path, &SqliteConfig::default())

    3. Update open_in_memory() to accept optional &SqliteConfig:
       - New method: open_in_memory_with_config(cfg: &SqliteConfig)
       - Existing open_in_memory() calls with default config

    4. Pass cache_size from config to pool builder if set

    Reference: RESEARCH.md lines 256-286 for config-aware pool initialization.
  </action>
  <verify>
    cargo check --lib passes, cargo test config_tests passes
  </verify>
  <done>
    open_with_config() reads pool_size, passes to PoolManager. Default config uses pool_size=5.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add PoolManager::with_max_size() constructor</name>
  <files>sqlitegraph/src/graph/pool.rs</files>
  <action>
    1. Update PoolManager to accept max_size parameter:
       - Add `pub fn with_max_size(max_size: usize) -> Result<Self, Error>`
       - Configure Pool::builder().max_size(max_size)
       - Build and return PoolManager wrapping the pool

    2. Keep existing `new()` for default size:
       - Calls with_max_size(5) internally

    3. Add getter method `pub fn max_size(&self) -> usize` for introspection

    Use r2d2 Pool builder pattern: Pool::builder().max_size(n).build(manager).
  </action>
  <verify>
    cargo check --lib passes, PoolManager compiles with with_max_size()
  </verify>
  <done>
    PoolManager::with_max_size() sets pool max size. new() defaults to 5.
  </done>
</task>

</tasks>

<verification>
1. SqliteConfig has pool_size: Option<usize> field
2. with_pool_size() and with_max_connections() builders exist
3. open_with_config() reads pool_size and passes to PoolManager
4. PoolManager::with_max_size() configures pool max size
5. Default pool size is 5 when not configured
6. All tests pass with configurable pool
</verification>

<success_criteria>
1. Users can configure pool size: SqliteConfig::new().with_pool_size(10)
2. Pool size is passed through to PoolManager during initialization
3. Default pool size of 5 is used when not specified
4. Configuration pattern matches existing SqliteConfig builder style
</success_criteria>

<output>
After completion, create `.planning/phases/19-concurrent-features/19-02-SUMMARY.md`
</output>
